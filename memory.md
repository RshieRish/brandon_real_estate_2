# Project Memory

## Architecture Decisions
- 2026-08-27: Atlas operations instructions are repository-owned at `hermes/skills/atlas-backend-operations/SKILL.md` and installed before Hermes startup from a manifest-pinned image asset. Installation is atomic, idempotent, unrelated-skill preserving, and fail-closed on hash/path mismatch. For current Command contacts, only `command_contacts_search` and `command_contact_audience_preview` are authoritative; admin URLs are navigation locators, `contacts_search` is Google Contacts only, and `KW Success Agent Roster 2025` is historical unless explicitly requested.
- 2026-08-27: Legacy Sydney recovery is explicit, exact-selector, dry-run-first, and review-only. Admission requires the reconciled private identity, exact session/message/content hash, visible unfinished user evidence, and acknowledged backfill lineage. It creates one deterministic local inbound and never scans automatically. Replays are idempotent; selector, content, lineage, supersession, terminal, canary, control, or role mismatches fail closed without exposing prompt content.
- 2026-08-27: `review_only` is local spool/claimed-run policy and never enters backend run-start payloads. It survives restart, allows only read-only context and current Command audience tools, and blocks every known or unknown mutation before executor entry while persisting a single canonical `not_delivered` denial. The only valid recovery outcome is a current audience count/checksum/reference/masked sample plus proposed subject/body, an explicit nothing-sent statement, and a stop for fresh Brandon approval; old send wording is not approval and no `/new` is required.
- 2026-08-27: Recovery development through Task 4 passed the complete changed-area matrix at `248 passed, 5 expected exact-checkout skips, 3 subtests`, with focused Ruff, compileall, diff, and generic credential checks green. Independent review added synthetic-continuation supersession, an explicit recovery read-tool allowlist, mandatory managed-skill admission, symlink-safe destination validation, and non-retryable policy-denial evidence. The twice-applied pinned overlay was byte-stable with relative-tree SHA-256 `7c8cab8cdedd3362f5cc0a24fee05ee3fb2302ad1ad90e49eae5a7414d49674b`, and local JSON-RPC verification returned the exact ordered 25 tools with none forbidden. This snapshot is not deployment or production-recovery evidence.
- 2026-08-27: PR `#29` merged the recovery release at main `551823e431aca9a4096c53f13d9861c8331af853`; Atlas deployment `d34f1581-e546-4979-bb78-13ba4ebc0c7e` reached `SUCCESS`, public health passed, live tools/list remained the exact ordered 25, and the installed managed-skill SHA-256 is `7a8711fab72c32792f0140d2e8c63a243d0d95f67ad2f49519c5ecd4af5b321b`. Exact recovery run `9e2f123a-23db-4c48-9a87-b757e11c5e4a` used only Command audience preview and made zero mutations, but its never-attempted durable tool-result row exposed a drain-liveness defect. A live-tail SQLite failure must never block the outbox or kill its worker; the test-first hotfix retries worker iterations and passes `250 passed, 5 skipped, 3 subtests`. Resume the same persisted row after deployment; never enqueue the recovery again.
- 2026-08-26: Sydney durable context Task 14 is live and complete. PRs `#14`-`#26` culminated in application-code merge `de986f5e9c3808fd1253c2532670bfd9f7ce9e65`; its backend `4a761b35-04b6-4ab2-8774-7ef20c5f072f`, worker `0b3bf3b5-ca82-4d26-b585-0502965fd7df`, and Atlas `34932fbf-e916-4032-97fa-50b9d477a815` rollout deployments reached `SUCCESS`. Later evidence-only merges may trigger byte-equivalent backend/worker redeploys, so deployment IDs are historical evidence rather than forever-current pointers. Backend/worker/Atlas health, worker readiness, sole head `85e8b7c9d4f1`, single Atlas gateway, and live exact ordered 25-tool JSON-RPC all pass.
- 2026-08-26: The protected Task 14 backup is `/Users/rishabnandi/brandon-real-estate-production-backups/sydney-task14-pre-migration-20260826T180513Z.dump`, mode `0600`, size `516102115`, SHA-256 `8a200bba3ca51af9eba43e028a838b90eaaf4ef51553a33e77c8c7b7a2252847`, with `732` PostgreSQL 17 restore-catalog entries. Preserve it. Backfill matched `8` sessions, `918` messages, `947` events, `455/455` tool calls/results, and global hash `9c7c960a59b959e13faa317fda987193d60b8416367212da249c4281a73be689`; the compacted proof has zero unacknowledged rows.
- 2026-08-26: The production retry canary proves one persisted `45s` wait, one acknowledgement, restart recovery, attempt `2` success, one exact final assistant event/reference/hash, zero tool invocations, released lease, cleared error state, and no unresolved run. Spool pending/active/claimed/final-delivery counts are zero and the synthetic marker was removed. Automatic recall stays under `16000` tokens with source-linked sections; Command search/audience preview are read-only, Command-only, checksummed, and masked.
- 2026-08-26: Projection range coverage is server-owned by the committed database claim. Gemini may be normalized only when it omits exactly one interior UUID from an otherwise unique ordered echo that retains both endpoints and whose fact citations use only echoed IDs. Endpoint or multiple omissions, reordering, duplicates, foreign IDs, and inconsistent fact citations still fail closed. A final post-merge gate found that a later 100-event range exhausted the strict `4096`-token output ceiling and returned no parsed object. PR `#26` keeps that ceiling and provenance contract intact while reducing the default immutable range to `50` events. Two consecutive deployed cycles advanced checkpoints/facts `41/35 -> 43/36`, reduced lag `155 -> 55`, reset failures `7 -> 0`, released every claim, and kept protected health `ready` with all `14` sessions reconciled.
- 2026-08-26: Pre-release Task 14 development-gate snapshot: automatic recall preserves the newest recent events at whole-event boundaries. Retrieval passes trusted `(event_id, rendered event)` entries directly into packet budgeting; it never reparses `[source:<uuid>]` text, because newer messages may quote older markers. A truncated multi-source recent section without structured boundaries fails closed instead of guessing provenance. The completed live rollout is recorded above.
- 2026-08-26: `AgentConversationEvent.search_vector` is server-generated in the ORM mapping, matching the Alembic-generated column behavior and preventing SQLAlchemy from sending `NULL` on ORM insert. A real migrated PostgreSQL 17/TLS regression commits an ORM event and proves the generated vector is non-null.
- 2026-08-26: Final pre-release evidence is `757 passed, 5 expected skips` across the changed-test matrix, `16/16` Atlas, and `227/227` against exact template `7224d7c` plus Hermes `77a1650`; the real JSON-RPC proof is exactly 25 ordered unique tools with the original 22 unchanged. Sole migration head is `85e8b7c9d4f1`; focused lint, compile, diff, and credential checks pass. Independent review and the final structured-boundary re-review are clean. This is development evidence, not proof of deployment.
- 2026-08-25: Sydney durable context Tasks 1-13 are complete in code but not deployed. PostgreSQL is canonical; Hermes `state.db` remains the local transcript; `/data/.hermes/sydney_spool.db` is a private WAL outbox/cache. Atlas persists redacted inbound evidence before backend/model work, keeps one stable logical Brandon conversation across automatic continuation, recalls at most 16,000 tokens, and never requires Brandon to issue `/new`, `/reset`, or `/compact`.
- 2026-08-25: Task 14 review fixes are development-complete but not deployed. Redaction covers direct, percent-encoded nested, and JSON-wrapped callback secrets; shadow sync copies only the current turn after the inbound-user boundary; recognized transport exception types retry without relying on message text; and failed tool output remains redacted canonical evidence.
- 2026-08-25: Tool execution is fenced by the exact running, unexpired lease owner. Mutating intent deduplicates across regenerated model call IDs by tool plus argument hash or hashed caller idempotency key, and the runtime updates the returned canonical call ID after a proven not-delivered retry. The model sees one provider-owned unprefixed `context_history_search`; the prefixed MCP identity-bearing variant is hidden. Fresh combined evidence is `227/227` with PostgreSQL 17 TLS and exact pinned Hermes/template sources; no production change has occurred from this review pass.
- 2026-08-25: Reconciliation is an authority boundary. Backend ingest returns content-free authoritative event receipts. A session is marked reconciled only when Atlas submits the exact event count and timestamp/UUID-ordered hash; every new event atomically clears that marker. Backfill exits nonzero unless local/acknowledged session, message, event, role, tool-call, tool-result, per-session, and global evidence all match and no backfill row remains unacknowledged. Retrieval must remain off on any mismatch.
- 2026-08-25: Hermes production source is double pinned: template `7224d7c1a4dcffe9304f49bc843f55716f5561b4`, official tag `v2026.5.29.2`, and official commit `77a1650c78a4cb1813d8a81fa1da40a15b6a3ec5`, with exact upstream file hashes. The overlay is atomic/idempotent and refuses unrelated dirty source. The MCP release contract is exactly 25 ordered unique tools: preserve the prior 22 unchanged and append only `context_history_search`, `command_contacts_search`, and `command_contact_audience_preview`.
- 2026-08-25: Task 13 evidence is Sydney-focused `148 passed, 10 local PostgreSQL skips`, no-network/reconciliation slice `35 passed`, full frontend `661 passed` plus green TypeScript, and full backend at the exact recorded unrelated baseline `2472 passed, 689 skipped, 3 deselected, 17 failed`. CI owns non-skippable PostgreSQL 16/TLS context cases and sole head `85e8b7c9d4f1`. No production change occurred; use `docs/deployment/hermes-railway.md` for Task 14 backup, migration, shadow ingest, exact backfill/reconciliation, promotion, acceptance, and evidence-preserving rollback.
- 2026-08-24: Command task bulk archive uses a 25-row client-side pager over the existing authoritative all-task ledger. Selection stores task IDs across pages; changing lifecycle visibility, status, or due-date filters clears it. Current-page selection is separate from the explicit `Select all N matching tasks` escalation so an administrator can see the scope before archiving.
- 2026-08-24: `POST /api/v1/command/tasks/bulk-archive` accepts 1..500 unique task IDs and per-task UUID/version identities, sorts them by task ID, and delegates every item to `crm_task_service.archive`. Mixed logical outcomes are returned per task; successes clear selection, failures remain selected with details, and uncertain transport triggers one authoritative read with no automatic write retry. This work is code-only and not deployed; no production data or feature flag changed.
- 2026-08-24: Gmail/Sydney Task 9 controlled production E2Es are green. A controlled received Gmail, caller-UUID Agent Control sent commitment, and direct Sydney draft became exactly Tasks `454`, `455`, and `456`; each has exactly one creation request and task source, and replay of the send, History input, clarification enqueue/answer, handoff/approval, and creation produced no duplicate. Confirmed task creation remained an explicit authenticated Command click.
- 2026-08-24: Production Command handoffs must be absolute URLs built from `COMMAND_PUBLIC_BASE_URL=https://www.soldwithsweeney.com`, with the one-time handoff only in the fragment. Fix `68d4db2` merged through PR #11 as main `ca544e6dc9e99c08b3cd9b2c498eefd0774d1cf5` after all checks passed. A relative path is not an acceptable Telegram deliverable even when browser routing can resolve it locally.
- 2026-08-24: Task 9 rollback is durable disablement, never evidence deletion. Backend deployment `c46df9ff-d4c1-48a5-8b3b-adb29e3b43af` and worker deployment `e4efa174-0d7b-4623-8f05-afbb8246d980` are healthy on `ca544e6`; both explicitly hold the Gmail, Sydney-question, and Instagram switches false. The worker heartbeat has no current job and the exact `/ready` contract passes.
- 2026-08-24: A Gmail `message_not_found` incident may be acknowledged only after an authorized operator verifies the exact provider message returns `404`; the database incident alone is insufficient. The post-fixture incident remains pending and alerted, its exact run/account recovery state is preserved, and Gmail intake remains disabled. Use `docs/deployment/gmail-sydney-task-intake.md` for the final-page cursor, reseed/backfill, same-run missing-message, Telegram uncertainty, live Atlas, and rollback procedures.
- 2026-08-23: Gmail/Sydney Task 8 is live and complete. Source `a53ff04421c395be54012398cc1dccecddf08f97` merged through PR #5 at `f3ed543359cac39c3caa66dd3d25592319f1b921`; backend deployment `e29cfc64-5b2d-4265-ad9d-0e0d7d00a226` and Atlas deployment `51f50a75-32ed-4ed7-bd88-5dc5bfde0988` reached `SUCCESS` with both public health probes at HTTP 200. The checked-in live JSON-RPC verifier proves exactly 22 ordered unique tools: the prior 16 unchanged plus the six review tools, with required UUID `gmail_send.request_id` and no actual approve/dismiss/create-confirmed/archive/restore tool.
- 2026-08-23: Task 8 rollout authority is live deployment evidence, not source or local tests. At that rollout, the direct Railway Member session was required because the then-saved token was unauthorized. Task 14 later used a separately supplied account/workspace token verified with Member SSH/deployment access on 2026-08-26; it was not persisted over the older ignored `.env.railway-sweeney.local` value. Use the wrapper or a direct session only when the same path can list and access `enchanting-perception`, and never print a credential. Railway SSH must run `hermes mcp test atlas_backend`, while raw JSON-RPC `tools/list` against `/app/atlas_backend_mcp.py` is the machine-readable gate because this Hermes CLI lacks `mcp tools list ... --json`. Current backend service ID is `37afead7-c55b-4ef2-8071-ca4da1c88992`.
- 2026-08-23: The authenticated Task 7 production gate preceded Atlas advertising. Fragment handoff removal occurred before exchange network activity; exchange only issued the separate approval stage and no Approve click occurred. Controlled fixtures were dismissed with `applied_task_id=null`, the CRM task count remained 453, pending review returned to zero, and the three rollout flags remain false. Task 9 is the only remaining numbered Gmail/Sydney plan task.
- 2026-08-22: Gmail/Sydney Task 7 is complete locally. The authenticated Command task-suggestion workspace uses the existing navigation registry and strict typed API contracts. Its review DTO carries bounded extractor/source/audit provenance plus a unique `resolution_requirements` list, so generic `missing_required_field` evidence never makes the UI guess between owner and task-detail decisions.
- 2026-08-22: Duplicate confirmation is version-bound authority. Summary generation batches live-sibling detection and at most two current-version Command edit events per suggestion, then applies the same audited evidence validator used by approval. A confirmed current version no longer asks again, an untouched sibling still does, and frontend preparation is disabled whenever any authoritative resolution requirement remains.
- 2026-08-22: Task 7 handoff security is pre-hydration and fragment-only. Closure memory holds the handoff until one exchange; Proxy rejects query secrets; unauthenticated and expired-token flows preserve an unused-link reopen notice without exchange. Neither exchange nor prepare approves a task. Stage-two approval remains memory-only and requires an explicit click.
- 2026-08-22: Post-approval UI state is server-authoritative. The ACK must match suggestion, exact next version, and request UUID; Command then refetches the applied row and its `approve`/`apply` audit evidence. If that read fails, the UI locks a terminal fallback at the ACK version and clears stale audit provenance. A lost response retains the exact request UUID for replay.
- 2026-08-22: Task 7 final evidence is exact frontend `47/47`, full frontend `647/647`, focused PostgreSQL 16/TLS backend/workflow `28/28`, green TypeScript, touched ESLint, production build, Ruff, YAML, diff, production-build security/browser checks, and independent review with no Critical/Important findings. Full frontend lint remains separately known-red at `20 errors, 20 warnings` outside scope. Nothing was deployed or enabled; Task 8 may prepare overlay code/tests, but production Hermes tool advertising remains gated on a separately verified Task 7 deployment.
- 2026-08-22: Gmail/Sydney Task 6 is complete locally. FastAPI registers 24 protected review/admin/Agent routes exactly once. Agent Control exposes exactly six narrow CRM capabilities: read tasks, read suggestions, answer the current clarification, create a draft, request a Command approval handoff, and submit a dismissal proposal. Agent cannot create a confirmed task or perform dismissal; authenticated Command remains the sole trusted authority for both.
- 2026-08-22: Task application is a two-stage, explicit Command act. Handoff and approval secrets are generated with `secrets.token_urlsafe(32)`, stored only as hashes, returned with no-store/no-referrer controls, prevalidated without input echo, and rotated so one handoff remains live. Approval locks Gmail thread before suggestion, revalidates unique contact authority, and calls `crm_task_service` inside the same transaction. Exact consumed-nonce replay survives expiry; draft and dismissal-proposal replays bind immutable original request identity even after later suggestion edits.
- 2026-08-22: Review blockers are resolved only by explicit Command fields, never incidentally by an edit. Edits/dismissals supersede pending clarification. Current-version duplicate resolution requires an exact linked Command audit containing `confirm_not_duplicate`; later unrelated edits invalidate it. Dismissal writes one exact suppression per attached Gmail instance, and admin reprocess consumes a receipt-bound audited suppression override exactly once.
- 2026-08-22: Task 6 final evidence is the exact Task 1-through-Task 6 PostgreSQL 16/TLS workflow `1192/1192` with 35 deprecation warnings in `169.66s`; focused Task 6 `37/37`, worker/deployment `40/40`, and compatibility `252/252` also passed. Ruff, compileall, workflow YAML, diff, sole head `84d7a5f9b2c3`, and independent review are clean. Nothing was deployed or enabled; Task 7 owns the authenticated Command suggestion-review UI.
- 2026-08-22: Gmail/Sydney Task 5 is complete locally at Alembic head `84d7a5f9b2c3`. The four new body-free evidence stores are `crm_task_clarifications`, `sydney_question_outbox`, `crm_task_suggestion_approval_nonces`, and `crm_task_suggestion_events`. Clarification/outbox/event identity is immutable, secrets persist only as hashes, downgrade refuses owned evidence, and rolling revision-83 writes are distinguished through omitted-column NULL sentinels so explicitly invalid current writes are rejected rather than silently repaired.
- 2026-08-22: Sydney owns one configured Telegram chat slot and asks only the highest-priority unresolved consequential field: action scope, contact, due time, owner, then task details. Answers are strict opaque-code plus expected-version operations on the same locked suggestion; every accepted answer increments the version before reevaluation. Old-version questions are superseded, deadline equality is late, five rounds force manual review, and no sixth question is created.
- 2026-08-22: Telegram clarification delivery is persist-before-send and immutable per attempt. Initial/retry/reminder attempts have deterministic dedupe identity; unknown delivery is reconciled before any explicit retry; one reminder is due 24 hours after known/reconciled delivery; the slot releases at the original 48-hour boundary. Retry evidence stores bounded reason hashes and an audit FK, never raw operator reason as event data.
- 2026-08-22: Contact uniqueness is an authority boundary, not a row-count hint. Revision `84` installs a database statement-level advisory lock for every contact identity INSERT, UPDATE, and DELETE; inference, preserved-contact reconciliation, clarification answers, revision-83 compatibility, and Task 4 approval preparation acquire the identical transaction lock before bounded locked uniqueness reads. Suggestions lock before contacts, duplicate/stale compatibility writes fail with SQLSTATE `23514`, and two-session answer/source plus duplicate-writer races are tested.
- 2026-08-22: Task 5 final evidence is the exact Task 1-through-Task 5 PostgreSQL 16/TLS workflow `1155/1155` with 19 unchanged `python-jose` warnings, focused touched suites `445/445`, clean Ruff/compileall/diff checks, sole head `84d7a5f9b2c3`, and independent review with no Critical or Important findings. Nothing is deployed or enabled; Task 6 owns authenticated approval/application and the protected operator routes.
- 2026-08-21: Gmail/Sydney Task 4 uses one strict model envelope: exact schema ID `gmail-task-v1`, top-level `schema_version` plus at most 20 actions, finite advertised semantic action/object enums, and no model-owned IDs, keys, candidate contacts, or authority. Subject and body are separately encoded untrusted evidence; provider `message_at` is the trusted relative-date anchor. Raw output, structured text, evidence previews, deadlines, exception frames, and cancellation lifetimes are bounded and body-free after extraction.
- 2026-08-21: Durable obligation identity is backend-owned. Reconciliation chooses contact identity only after exact unique CRM lookup and actual-email recanonicalization; otherwise it uses receipt-bound participant HMAC evidence, with missing/multiple/self-copy parties, taxonomy fallback, and ambiguous due/owner meanings message-scoped. Exact base/fingerprint/instance/material attaches; a different fingerprint creates `possible_duplicate` siblings; a different text-instance digest under the same fingerprint produces one `multiple_actions` manual container without overwriting its primary payload. Suppression includes the exact instance digest so dismissal of one task cannot erase another.
- 2026-08-21: Task 4 reconciliation requires the exact current receipt processing lease, takes the stable account/thread transaction lock before protected evidence/suggestion work, and persists append-only obligations/source links with an immutable suggestion-or-suppression disposition. Revision `83` now enforces source/disposition binding, exact duplicate scope and no-self links, one-use exact-receipt audited suppression override, and generic-plan-safe indexes for candidate, membership, authority, and replay queries. All model-driven work is bounded to 20 actions and one capped candidate batch.
- 2026-08-21: Task 4 creates review suggestions only and asserts zero `CRMTask`, `CRMTaskCreationRequest`, `CRMTaskSource`, `CRMTaskLink`, `CRMActivity`, and CRM task lifecycle writes. `CRMTaskSuggestionService.task_payload` is valid only inside the same open transaction after DB-derived scope lock, locked reload, payload recomputation, version/state/blocker/sibling checks, and unique-contact revalidation; Task 6 must perform authenticated creation before releasing that transaction.
- 2026-08-21: Task 4 final evidence is PostgreSQL 16.13/TLS migration `40/40`, reconciliation `159/159`, extractor `136/136`, touched compatibility `236/236`, and independent exact Task 4 aggregate `335/335` with **QUALITY APPROVED** and zero open Critical/Important findings. The exact prior Task 1-through-Task 3 workflow passed `577/577` with 19 unchanged `python-jose` deprecation warnings; the dedicated workflow was then expanded through Task 4 and its repository contract passed `2/2`. Sole head, py_compile, Ruff, and diff checks passed; the owned cluster was removed. Nothing was deployed or enabled.
- 2026-08-21: Task 9 owns extraction failure terminalization and worker composition. It must persist sanitized body hash/classification on the active processing lease before Task 4 claim/reconcile, catch Task 4's fixed invalid-model-output error, call `fail_attempt(category="invalid_model_output")`, and prove failed terminal state, zero partial evidence/suggestion/source writes, and bounded retry without an N+1 attempt. Task 4 source identity checks must not be weakened to compensate for Task 9 ordering.
- 2026-08-21: Gmail/Sydney Task 3 shipped locally in commit `a864955`. Gmail History owns one direct `NullPool` PostgreSQL connection and session advisory lock per account across page commits; backend PID/lock ownership is verified before provider work and during release. Receipt/page evidence commits before the account cursor CAS, retryable failures resume their durable page token, fatal failures block with a deduplicated durable alert, and bounded backfill promotes only an exact completed recovery snapshot.
- 2026-08-21: Gmail provider transport is truly single-attempt. Google API retries, authorized-HTTP 401 replay, httplib2 reconnect replay, and redirects are disabled; synchronous work stays in the bounded shared executor under socket and outer deadlines. Message/thread/history IDs, page tokens, headers, MIME shape, base64url content, and body budgets fail closed. Raw bodies exist only through metadata-first sanitize/observation and are deleted before consumer finalization, database commits, logs, or retained exception frames; participant addresses persist only as HMACs.
- 2026-08-21: Agent Gmail sends require a caller UUID, a committed intent and audit before provider I/O, canonical unresolved-send uniqueness, and no automatic retry after uncertainty. Provider message/thread identity is immutable. A provider result that conflicts with already committed History evidence atomically preserves the History identity, quarantines and version-bumps the origin under its row lock, and blocks same-request replay without another provider call. Receipt enrichment locks in origin-to-receipt order and never regresses `processing`/`processed` lifecycle state.
- 2026-08-21: Delete-between-History-list-and-fetch recovery is represented by the twelfth revision-83 table, `gmail_missing_message_incidents`: body-free exact run/message/thread/page identity, incident-owned durable alert state, authenticated admin detail and acknowledgement, and exact same-run resume without cursor loss. Bound Workspace OAuth is DB-authoritative and generation-checked under the binding advisory lock across poll/profile/backfill/receipt failure persistence; stale credentials cannot poison a rotated binding.
- 2026-08-21: Task 3 final evidence is PostgreSQL 16/TLS History+origin `154/154`, final race barriers `9/9`, and exact 17-file workflow `567/567` with 19 warnings in both implementation and independent review runs. Ruff, compileall/py_compile, diff, and sole-head checks passed; independent **SPEC PASS** and **QUALITY APPROVED** found zero remaining Critical/Important issues. Disposable clusters were removed. This is not deployed or enabled: keep `GMAIL_TASK_INTAKE_ENABLED=false`, `SYDNEY_TASK_QUESTIONS_ENABLED=false`, and `INSTAGRAM_INTEGRATION_ENABLED=false` until later rollout gates.
- 2026-08-21: Gmail/Sydney persistence Task 2 shipped locally in commit `1a691157`. Alembic revision `83c6f4e8a1b2` is the sole head directly after `82b5e3d7f0a1` and owns exactly eleven Gmail/task-intake tables. The implementation changed exactly nine files: the migration, ORM models, strict schemas, migration contract, model/Alembic registration, the two revision/workflow compatibility tests, and the dedicated workflow. It added no Task 3 adapter, route, or worker job.
- 2026-08-21: Gmail evidence authority is relational, not string-conventional. Suggestions carry nullable account/thread identity only for `gmail_message`; Sydney drafts carry a globally unique request UUID and no fabricated Gmail identity. Source rows use composite foreign keys so suggestion, receipt, obligation, account, thread, and direction cannot be cross-wired. Workspace emails must equal `lower(trim(workspace_email))`; suggestion and suppression source types are exactly `gmail_message|sydney_chat`; a linked backfill run must belong to the same account, while Task 3 must still verify `run_kind=backfill` before cursor promotion.
- 2026-08-21: Revision 83 persists hashes and bounded previews only, never raw bodies or credentials. The exact NULL-safe unresolved-send partial uniqueness serializes competing unresolved intents; History-inferred `human_send` rows must be succeeded with provider IDs; application state/result shape and provenance are database-enforced. ORM UUID defaults and array types remain SQLite-bootstrap safe without changing PostgreSQL DDL. Downgrade takes a deterministic all-table `ACCESS EXCLUSIVE` lock before its evidence guard and refuses every nonempty owned table while preserving existing CRM rows.
- 2026-08-21: Final evidence for Task 2: real PostgreSQL 16/TLS `29 passed`; exact Task 1+Task 2 workflow `95 passed`; real PostgreSQL CRM lifecycle `13 passed`; Ruff, compilation, cached diff, and diff checks clean; independent **SPEC PASS** and **QUALITY APPROVED**, including an independent `29 passed` and zero-relation cleanup check. The exact disposable database was empty, the cluster was stopped, port `55434` closed, and its validated directory moved recoverably out of `/tmp`. This is not a rollout: `GMAIL_TASK_INTAKE_ENABLED=false`, `SYDNEY_TASK_QUESTIONS_ENABLED=false`, and `INSTAGRAM_INTEGRATION_ENABLED=false`; no deployment, production migration, provider call, or live enablement occurred.
- 2026-08-20: Shared integration runtime Task 1 shipped locally in commit `4ffed4c`. Alembic has one head, `82b5e3d7f0a1`, directly after `81a4d2c6e9f0`. Final evidence: PostgreSQL 16.13 TLS Task 1 `66 passed`, developer/no-PostgreSQL `33 passed, 33 skipped`, existing CRM lifecycle `165 passed`, legacy notification `5 passed`, clean compile/diff checks, independent **SPEC PASS**, and **QUALITY APPROVED** with no remaining Critical or Important findings.
- 2026-08-20: Integration-worker liveness is dependency-free `/health`; `/ready` is a sanitized, bounded, read-only view of database reachability, sole migration head, fresh scheduler-owned heartbeat, and initialized job registry. The worker uses deterministic `FIRST_COMPLETED` supervision, cancels and awaits its peer, prefers peer exceptions over a normal return, and leaves no orphan task. Scheduler heartbeat cadence is independent from per-job due cadence, and every job honors its own `interval_seconds`.
- 2026-08-20: Notification retries claim and commit only one row before provider I/O, immediately revalidate owner/state and renew that lease, then enforce a 30-second outer provider timeout below the default 120-second lease before claiming another row. This prevents later batch rows from expiring while a prior SMTP call stalls; delivery remains deliberately at-least-once after a crash.
- 2026-08-20: Task 1 PostgreSQL destruction is fail-closed: exact `_test` database and TLS/PostgreSQL checks, an empty-schema precondition, and one cryptographic ownership marker are mandatory. Downgrade, cleanup, schema drop, and reuse require the exact `(1, 1)` ownership tuple; Python `-O` regressions prove wrong, missing, duplicate, foreign, or populated ownership states preserve sentinels. `.github/workflows/crm-task-lifecycle-migration.yml` and `backend/.env.example` were correctly left unchanged because their existing behavior already met the contract.
- 2026-08-20: Task 1 is not a rollout. `GMAIL_TASK_INTAKE_ENABLED=false`, `SYDNEY_TASK_QUESTIONS_ENABLED=false`, and `INSTAGRAM_INTEGRATION_ENABLED=false`; `backend/main.py` was not changed, no integration loop runs in the FastAPI web process, and no deployment or production enablement occurred.
- 2026-08-20: Gmail/Sydney intake contracts were hardened before implementation in documentation commit `0a4c1fa` after preflight and adversarial review found unsafe ambiguity. Independent outcome is **SPEC PASS** and **QUALITY APPROVED**. This was not a deployment: keep `GMAIL_TASK_INTAKE_ENABLED=false`, `SYDNEY_TASK_QUESTIONS_ENABLED=false`, and `CRM_TASK_ARCHIVE_ENABLED=false` until the implementation and live rollout gates pass.
- 2026-08-20: Gmail sends persist a `sending` intent/audit before a zero-retry provider call. An exact NULL-safe PostgreSQL partial index permits only one unresolved `(account_id, canonical_send_hash)`; a new UUID cannot bypass `sending`/`delivery_uncertain`, and a successor must bind the old intent after authenticated `not_delivered`. Gmail History serializes per account and obligation reconciliation serializes per account/thread through versioned, fixed-vector advisory keys. A delivered reconciliation is eligible only after transient verification of account, `SENT`, message/thread, and canonical envelope/body.
- 2026-08-20: Sydney asks one consequential question at a time. Every initial/retry/reminder delivery is durable and immutable; there is one reminder at 24 hours, a fixed 48-hour release, no repeated field/version, and a five-round ceiling. Hermes identity/correlation is untrusted and may only update review drafts or submit a dismissal proposal; actual dismissal/suppression and task application remain authenticated Command-only.
- 2026-08-20: Command approval has two issuance paths: a fragment-only 15-minute handoff exchanged by an authenticated administrator, or ordinary authenticated `/approval/prepare`; both issue a separate five-minute approval nonce and require an explicit later Approve click. Handoff/approval tokens come from `secrets.token_urlsafe(32)`, persist only unique SHA-256 hashes, and reject malformed/short/noncanonical input before lookup. The Hermes bridge must preserve its exact existing 16 MCP tools and add exactly `crm_tasks_read`, `crm_task_suggestions_read`, `crm_task_clarifications_answer`, `crm_task_drafts_create`, `crm_task_suggestions_approval_link`, and `crm_task_suggestions_dismiss_proposal`; trusted writes remain absent.
- 2026-08-20: Gmail/Sydney implementation cannot begin without the CRM foundation gate, fail-closed disposable real PostgreSQL 16 CI, one serial Alembic head, real two-session races, bounded executor-offloaded provider calls, a `FIRST_COMPLETED` scheduler/health-server supervisor, scheduler-owned heartbeat with read-only `/ready`, exact FastAPI router registration, scoped Task 7/8 frontend/Hermes CI, and controlled live deployment evidence. Code/schema rollback keeps durable evidence unless an explicit audited destructive export procedure is used.
- 2026-08-20: Task archive browser acceptance uses one authenticated authoritative task ledger for exact active/archived/all reads and single-attempt archive/restore commands. Historical lifecycle replay returns an immutable stored ACK even after the current task advances and must never increment or mutate current state again. Acceptance must prove trimmed reason, preserved workflow status, version correlation, fresh Restore identity, conflict, and both uncertain-response reconciliation branches.
- 2026-08-20: Task 8 implementation `011a183` is scoped green: Playwright `7 passed` with `14` intentional project skips, the real TLS PostgreSQL task contract passed `164`, Alembic has sole head `81a4d2c6e9f0`, frontend passed `614`, and TypeScript, Task-8-only ESLint, build, and diff checks pass. Archived-state WCAG requires stable colors during selection and at least `text-white/60` for the explanatory copy. Do not describe the whole repository as green: full backend was `1822 passed, 1 skipped, 3 deselected, 11 unrelated failures` (Calendar corrected to `3/3` with dummy env), and full ESLint still has `20 errors and 20 warnings` outside Task 8. No live deployment or authenticated target check occurred; keep `CRM_TASK_ARCHIVE_ENABLED=false`.
- 2026-08-20: Contact workspace mutable tasks use one authoritative ten-field lifecycle row (`id`, `title`, `contact_id`, `description`, `priority`, `due_at`, preserved workflow `status`, `archived_at`, `archive_reason`, `version`). The frontend accepts only the exact rolling legacy seven-field row or exact lifecycle row; `archived_at` determines lifecycle visibility. Recovered source-only archived occurrences are immutable `Recovered evidence`, while legacy archived rows stay read-only.
- 2026-08-20: Contact Restore is server-authoritative: one UUID/version request, no automatic retry, mandatory internal-workspace plus summary reconciliation, exact-original gating for same-request Retry, and exact authoritative comparison after a retry ACK. Task 7 verification passed `411` combined backend tests, `182` affected frontend tests, the prior `614`-test full frontend suite, TypeScript, scoped ESLint, and diff checks at `0a124a3`. Deploy the compatible frontend before the additive backend and keep `CRM_TASK_ARCHIVE_ENABLED=false` until Task 8 and authenticated target verification pass.
- 2026-08-20: Task lifecycle UI is ACK-only and server-authoritative. Archive/Restore uncertainty performs one fresh all-task read and never auto-retries; a manual Retry reuses the exact lifecycle request and is confirmed only when the authoritative row matches the returned ACK. Newer same-state history is treated as another actor's change and never exposes Undo. Archived rows expose Restore only, and every disappearing recovery control hands focus to a live announced target.
- 2026-08-20: Every browser task mutation uses the strict shared task client and is single-attempt. A logical create retains one UUID, canonical payload, and IANA timezone across uncertainty and explicit retry; canonical payload changes rotate the identity. Task, link, and structured-conflict responses are bounded to PostgreSQL `int32` and correlated to the requested resource/version/state before the UI adopts them. Reconciliation unlocks writes only after an authoritative Tasks read, never a partial aggregate result.
- 2026-08-20: Task Archive/Restore is reversible and separate from workflow status, with one optimistic version shared across PATCH, new links, archive, and restore. Historical replay is exact, same-state requests record no-op events, active is the default visibility, every write goes through `crm_task_service`, and the feature flag defaults off.
- 2026-08-20: Task 5's atomic typed frontend is the rollout gate. Link listings lock the parent task row for a coherent version; task integer inputs are bounded to PostgreSQL `int32`, maximum-version mutations return `task_version_conflict`, and destructive test cleanup requires an exact `_test` target, empty user objects, and exactly one matching ownership marker even under `python -O`.
- 2026-08-18: Every production CRM task create must go through `services/crm_task_service.py`; direct `CRMTask(...)` writes outside that service are forbidden. The service atomically persists the idempotency claim, task, provenance, optional contact activity, and lifecycle event without owning the outer commit/rollback. Command requires an admin-derived actor and UUID `X-Idempotency-Key`; future Gmail/Sydney writers must use the same typed actor/source contract.
- 2026-08-18: Archive task imports require immutable `source_id` plus `source_row_id`; derived UUID5 keys are processed in sorted order to prevent reversed-order transaction deadlocks. Mutable task fields and authenticated actor remain in the canonical payload hash, so edits/cross-admin retries return structured conflicts rather than duplicates. The import template intentionally leaves identities blank and refuses submission until they are replaced.
- 2026-08-18: The real TLS PostgreSQL task contract is CI-enforced for migration, service, API, concurrency, outer transaction, auth, and configuration changes. Test databases must have an exact `_test` suffix and pass the ownership/empty-schema gate; the documented async URL uses `ssl=require`, while its `psql` derivative must translate that key to `sslmode=require`.
- 2026-08-18: All CRM task reads use `services/crm_task_projection.py`: archive visibility dominates workflow, active means only non-archived `open`/`in_progress`, and cancelled is never open. Contact summary exposes `archived_mutable_tasks` and `archived_recovered_evidence` plus their combined `archived_tasks` total. For rollout, the new frontend accepts legacy and expanded summaries; deploy it before the backend because the old strict decoder rejects additive keys.
- 2026-08-18: CRM task lifecycle persistence is revision `81a4d2c6e9f0`. Archive visibility is stored in `archived_at/by/reason` and is never encoded as workflow status; all task mutations will increment optimistic `version`. Creation requests, task sources, and lifecycle events are durable/replay-safe. The migration locks both normalized tasks and recovered-source occurrences before count capture, uses bounded transaction timeouts, assigns `CURRENT_TIMESTAMP` only when both legacy timestamps are null, and refuses downgrade once durable lifecycle history exists. Its real Alembic chain is required in TLS PostgreSQL CI.
- 2026-08-18: Implementation is decomposed into four test-first plans. The database changes must remain one serial chain: `7d1f3a5b6c8e` -> `81a4d2c6e9f0` task lifecycle -> `82b5e3d7f0a1` integration runtime -> `83c6f4e8a1b2` Gmail intake -> `84d7a5f9b2c3` Sydney review -> `85e8b6a0c3d4` Instagram cache -> later CRM lifecycle migrations. Do not create parallel heads.
- 2026-08-18: Initial Sydney CRM authority is deliberately narrow: read suggestions/tasks, create drafts, answer one pending clarification, generate a Command approval link, and dismiss. Confirmed create/archive/restore stays out of Hermes until a trusted signed inbound channel assertion exists; authenticated Command performs final task approval.
- 2026-08-18: Instagram production enablement requires a newly authorized long-lived Page token; the token pasted on 2026-08-17 was exposed and expired and must never be stored or deployed. Reliability means backend bearer auth, last-good cache, early expiry/version alerts, and explicit degraded state, not a false promise that Meta credentials can never be revoked.
- 2026-08-17: The approved email-task model automatically analyzes both received and sent Gmail into reviewable suggestions, but requires Brandon's approval before task creation during initial rollout. A repo-owned worker sends one concise private question through the Sydney Telegram bot only for consequential ambiguity, durably resumes the same suggestion after an answer, and sends at most one reminder. Initial final approval stays in authenticated Command UI unless Hermes later supplies a trusted signed Telegram identity assertion.
- 2026-08-17: Task removal is reversible Archive and Restore, separate from workflow status. Other top-level CRM records use entity-specific lifecycle actions; there is no generic hard delete. Hermes reaches CRM only through typed audited agent-control actions, never an admin JWT or direct database credentials.
- 2026-08-17: Gmail automation uses a dedicated restart-safe History worker, durable receipts and deduplication, structured extraction, bounded metadata, and no persisted raw email body. Instagram fetching moves into FastAPI/Railway with authorization headers, a supported pinned Graph version, last-good cache, health and alerts. The pasted 2026-08-17 token was exposed and already expired; never deploy or retain it.
- 2026-08-12: `/admin/command` implementation lives on isolated branch `feat/command-workspace`. Its first committed slice (`a7ccf66`) adds internal CRM SQLAlchemy models rather than replacing legacy lead data. Contacts link to `leads.id`; agreements are internal lifecycle records, not legal DocuSign execution.
- 2026-08-12: Command now has live internal API-backed workspaces for contacts, tasks, Smart Plans, opportunities, agreements, and listings, plus connected entrypoints into existing Content/Funnels/Analytics. Sweeney AI is a deterministic, review-required internal briefing endpoint; it does not take actions or send CRM data to an external platform.
- 2026-08-12: The configured PostgreSQL database was migrated through Command revision `f3a91c2d7e10`; all new Command tables now exist without altering legacy records.
- Next.js App Router: SSR + static pages, no API routes (UI only)
- FastAPI backend: all AI, DB, business logic
- Gemini flash for chatbot speed; Gemini pro for complex analysis
- Neon PostgreSQL for launch
- 2026-04-10: Approved direction for Brandon internal notifications is a DB-backed `notification_jobs` queue with save-first semantics and retry-until-delivered email behavior instead of inline route-level SMTP sends.
- 2026-04-10: Implemented `notification_jobs` with DB-backed retry state plus a background retry loop in `backend/main.py`, so failed Brandon-notification emails can continue retrying independently of new traffic.
- 2026-05-12: Auto-blog posting runs as a second in-process asyncio loop in `backend/main.py` (`_blog_auto_post_loop`), gated by `BLOG_AUTO_POST_ENABLED` and paced by `BLOG_AUTO_POST_INTERVAL_HOURS` (default 72h). Restart-safe via a `MAX(created_at) WHERE is_posted` check before every post. Existing HTTP `/api/v1/blog/cron` endpoint still works for ad-hoc admin triggering.
- 2026-05-12: Blog Gemini stages use `gemini-3-flash-preview` (research), `gemini-3-pro-preview` (writing), `gemini-3-pro-image-preview` (cover image). All three are valid against the prod API key — confirmed via direct `models?key=…` listing and live `generateContent` calls. Do **not** "fix" these to `gemini-3.1-*` to match `services/gemini.py`; they are intentionally distinct models.
- 2026-05-14: Cover-image upload requires the six `R2_*` env vars (`R2_ENDPOINT`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`, `R2_BUCKET_NAME`, `R2_PUBLIC_URL`, `R2_REGION`) on **both** local AND Railway. Local had them; Railway didn't until 2026-05-14 — that's why every Railway-generated post landed without a cover image until the fix. Diagnostic surfaces kept in place: `POST /api/v1/blog/admin/test-image` (admin JWT) returns `{image_url, error, took_ms, r2_configured, r2_endpoint_set, r2_public_url_set}`; `blogs.image_gen_error TEXT` column captures the failure reason of every auto-post that has `image_url IS NULL`. Use these first whenever a blog ships without a hero.

## Integration Status
## Integration Status
- Gemini API: Key provided; using `gemini-1.5-pro` (pro) and `gemini-1.5-flash` (standard).
- Google OAuth/Calendar: OAuth client credentials are present, but as of 2026-04-10 Brandon still needs to complete the one-time Google consent to generate `GOOGLE_CALENDAR_REFRESH_TOKEN`.
- Instagram: Access token prone to expiration; frontend handles fetching failure gracefully.
- Railway Backend: Docker builder forced via `railway.json`. Uses dynamic `$PORT` and root context.
- 2026-05-31: Historical Railway work used `scripts/railway-sweeney ...`, which sources the gitignored `.env.railway-sweeney.local` account/workspace token. That saved token failed the 2026-08-23 rollout and was not overwritten during Task 14. A separately supplied account/workspace token was verified with Member access on 2026-08-26; live project listing, project/service status, SSH, and deployment evidence remain mandatory for whichever authentication path is used.
- 2026-06-01: Approved architecture for Brandon AI / Atlas assistant is same Railway project, separate Hermes service. Keep Hermes under `enchanting-perception` as `atlas-agent` (or `brandon-hermes` if name availability requires), and let it control the existing backend only through explicit `/api/v1/agent-control/*` endpoints protected by `AGENT_CONTROL_TOKEN` and audit logging. First slice is read-only status/actions/recent leads/recent bookings; no Gmail/Calendar/Drive/CRM/outbound autonomy until later specs.
- 2026-06-01 implementation note: FastAPI now has a read-only agent-control bridge guarded by `AGENT_CONTROL_ENABLED` and `AGENT_CONTROL_TOKEN`. Routes are `/api/v1/agent-control/status`, `/actions`, `/leads/recent`, and `/bookings/recent`; lead/booking responses mask email/phone and booking summaries expose `has_google_event` instead of `google_event_id`. Audit rows use `agent_action_audits`. Hermes deployment instructions live in `docs/deployment/hermes-railway.md`.
- Vercel Frontend: Requires `NEXT_PUBLIC_API_URL` to match Railway domain.

## Content Status
- Videos: Available in public/assets/
- Headshots: Available in public/headshots/
- Logos: Available in public/logos/
- House blast frames: DONE (Task 22) — 60 WebP at frames/frame_001.webp, 60 JPEG at frames/house-blast/frame_0001.jpg
- Bio/reviews: In BRANDON_RE_SPEC.md Section 14

## Known Issues
- As of 2026-05-19, production blogs are down because the Railway blog API cannot connect to Neon: the live response says the Neon account/project exceeded compute time quota, and the DB connection should use `sslmode=require`. `/blog` depends on `GET /api/v1/blog/?limit=50`, while `/blog/[slug]` returns 404 when the server-side blog fetch fails. The local Neon host is `ep-tiny-firefly-ankqqw49-pooler.c-6.us-east-1.aws.neon.tech` (`neondb`, `neondb_owner`); use `ep-tiny-firefly-ankqqw49` to match the project/endpoint in Neon or Railway. That host still resolves and Railway gets a Neon-origin quota error, so the endpoint exists/routes in Neon rather than being moved elsewhere. Strongest local account candidate is `millergid9@gmail.com`: Chrome Profile 13 signed into Neon, opened org `org-long-bread-73501543` / project `nameless-credit-95836299?database=neondb`, and the exact `ep-tiny-firefly-ankqqw49` URL was pasted into the Brandon project chat about one minute later. This is high-confidence local forensic evidence, not server-side Neon ownership proof. Restore/upgrade Neon compute, add SSL mode to Railway `DATABASE_URL`, restart backend, then verify the blog list and a known slug.
- Real Google Calendar event creation is still blocked until Brandon completes the one-time OAuth connect flow and the backend receives a refresh token.
- As of 2026-04-10, the immediate `Access blocked` error on the Google Calendar connect flow is specifically caused by `redirect_uri_mismatch`: the backend is still generating the OAuth URL with the localhost callback because `GOOGLE_CALENDAR_REDIRECT_URI` is unset and falls back to `http://localhost:8000/api/v1/booking/calendar/callback`.
- The next required setup step is to point `GOOGLE_CALENDAR_REDIRECT_URI` at the real public backend callback and register that exact URI in the Google Cloud OAuth client.
- As of the later 2026-04-10 live booking verification, the redirect mismatch was fixed, but a separate persistence issue remained: the OAuth callback currently writes the Google Calendar refresh token into the container-local backend `.env`, which is not durable on Railway across deploys / instance changes.
- Local code now also stores the refresh token in the `settings` table and reloads it from DB before live calendar reads/writes, but that backend code still needs deployment and then one more Google reconnect to repopulate the durable token store.
- A real test Google event was accidentally created on Brandon's calendar for Monday, April 13, 2026 at 9:00 AM ET during the failed pre-migration booking attempt; that slot disappeared from availability even though the DB write failed.
- Booking emails are only sent after the full booking flow succeeds. So when live bookings fail earlier in the pipeline, Brandon will not receive any SMTP notification even if Gmail SMTP is configured correctly.
- Current email behavior also masks delivery failures: `_send_email()` returns `False` on SMTP failure, but the booking route does not check that return value, so the app does not currently surface email-delivery failure to the UI or API response.
- As of the notification-queue implementation on 2026-04-10, Brandon notifications no longer depend on inline route-level SMTP helpers in the new code path; they are queued and retried from `notification_jobs`. This still needs a backend deploy before production uses it.

## Admin Auth Notes
- As of 2026-04-10, the Brandon admin login issue was traced to seed drift: the `admin_users` row already existed in the live database with an older hash that did not match `changeme123!`.
- `backend/seed.py` now uses `ensure_admin_user(...)` to create the Brandon admin account if missing and also refresh the stored hash if the existing row no longer matches the expected seeded password.
- The connected database was reseeded on 2026-04-10, and a direct `/api/v1/auth/login` smoke test succeeded for the seeded admin account from `seed.py`.

## Chatbot Architecture
- Chat widget is mounted globally from `frontend/src/components/layout/ClientWidgets.tsx`, so it appears across public pages.
- `backend/routers/chat.py` now returns structured chat payloads: `text`, `actions[]`, `widget`, plus backward-compatible `response`.
- `backend/services/gemini.py` instructs Gemini to return JSON and normalizes assistant actions into three allowed types: `send_message`, `navigate`, and `open_widget`.
- Legacy booking tags like `[BOOK_MEETING]` still work as a fallback and are normalized into `widget: 'calendar_picker'`.
- `frontend/src/hooks/useChat.ts` normalizes the structured API payload, preserves legacy booking fallback, and stores `actions` on assistant messages.
- `frontend/src/components/chat/ChatPanel.tsx` renders assistant messages as text bubbles with premium action buttons underneath and conditionally mounts `CalendarPickerCard` below a message when `widget === 'calendar_picker'`.
- `frontend/src/components/chat/CalendarPickerCard.tsx` remains the inline booking widget used by both direct widget replies and action-button triggered booking flows.
- Booking availability now enforces Monday-Friday `9 AM-6 PM` Eastern only, revalidates the selected slot against Brandon's live calendar before saving, and truthfully surfaces when Google Calendar still needs authorization.
- For in-person meetings, booking checks the locations on Brandon's neighboring calendar events and uses route-time estimates to hide infeasible travel slots.
- `/api/v1/chat/lead` exists, but the current chat frontend does not submit lead capture there.
- Current allowed navigate destinations are `/buy`, `/sell`, `/invest`, and `/about`.
- Generative UI is still best treated as a later-stage extension for multiple rich widget types; the implemented server-driven structured actions are the safer current fit.

## Calculator Notes
- Investor calculator now shows live numeric results before any lead gate. The gate is only for the deeper AI report generated from `/api/v1/investor/analyze`.
- Investor full-report gate now collects `name`, `email`, and `phone`, then renders AI explanation text, hold scenarios, exit-path commentary, and sensitivity cards once unlocked.
- Changing investor deal inputs after unlocking clears the prior full report so stale analysis is not shown.
- Investor calculator percentage inputs were corrected on 2026-04-10 to use human-entered percentages (`15`, `7`) instead of decimal fractions.
- Investor flip math now includes estimated closing costs in the instant snapshot and exposes `Holding Costs`, `Closing Costs`, and `Total Project Cost` cards. Current assumptions: `1.5%` buy-side closing costs and `1.25%` sell-side closing costs.
- Seller valuation tool now returns `calculation_id` from `backend/routers/evaluator.py`.
- Every seller valuation run is persisted as an analytics event with `event_type="seller_evaluator_calculation"` and includes the request inputs plus returned estimate in `metadata`.
- Seller feedback is now captured as a second analytics event with `event_type="seller_evaluator_rating"` linked back to the originating `calculation_id`.
- Current seller rating choices are:
  - `expected`
  - `under`
  - `above`

## Frontend Content Notes
- About page stats strip was updated to use Brandon-specific milestones instead of generic numbers.
- About page awards section now includes text-based recognition cards for MAR Good Neighbor, NEAR Good Neighbor, and Distinguished Young Professional.
- The requested `Brandon and Paige at Maine gala` image was added on 2026-04-02 at `frontend/public/headshots/brandon-and-paige-maine-gala.jpeg` and now replaces the lower About-page image in `TeamSection`.
- An awards-collage image is still not present in the repo as of 2026-04-02, so that swap remains blocked on asset delivery.
- Buy page common-mistakes section is now an interactive flashcard treatment rather than static cards.
- Buy lead-section gold headline text is now `THE ONE`.
- Seller lead-section headline is now `Stop Listing. Start Moving.`
- Home hero video no longer uses Brandon's headshot as a poster; it fades in only after the video loads to avoid the initial face flash.
- MS fundraising copy now uses `advocacy` instead of `research` on the relevant public sections.

## Buyer Components
- `MonopolyJourney`: 3-phase accordion — Phase 1 (DeviceMobile), Phase 2 (HeartStraight), Phase 3 (House); CaretDown toggle; AnimatePresence open/close
- `BuyerMistakes`: 4-card 2-col grid; XCircle (red-400) for mistake, CheckCircle (gold) for fix

## LeadCaptureForm Props
- Now accepts: `source?: string`, `leadType?: string`, `ctaText?: string`

## About Page Components
- All 7 sections are named function components in `frontend/src/app/about/page.tsx`
- HeroSection: split grid, AnimatePresence, Brandon Sweeney Headshot.jpg, floating glass name badge
- StatsStrip: 4-cell `grid-cols-4` glass cards with gold stat value + uppercase label
- BioSection: sticky left column (zoomed headshot), 3 glassmorphism panels right
- DesignationsSection: 5-logo grid (NEAR/MAR/NAR/GREEN/C2EX) + 2 award cards
- MSisBSSection: $300K stat, external link to MSisBSNewEngland.com, story glassmorphism panel
- TeamSection: SWS TEAM Headshot.png, asymmetric image-left text-right layout
- ContactSection: centered, 3-col contact grid (Phone/Email/Office), gold Book a Call CTA

## Admin Pages (Task 20)
- `frontend/src/app/admin/leads/page.tsx` — filter pills, table, right-panel detail with status PATCH + notes PATCH on blur
- `frontend/src/app/admin/content/page.tsx` — 2-col grid, inline edit mode per block, PUT on save
- `frontend/src/app/admin/funnels/page.tsx` — table with publish/copy-link, create form with AI loading spinner
- `frontend/src/app/admin/analytics/page.tsx` — stats strip, top pages + top events with gold bar, recent events table
- `frontend/src/app/admin/settings/page.tsx` — 3 integration cards (Gemini/Calendar/KW), password form (coming soon), site info
- Settings page Google Calendar card is dynamic as of 2026-04-10: it fetches live connection status, can open the Google Calendar OAuth flow, and supports status refresh after authorization.

## Last Session Context
- 2026-08-12: The approved next product surface is a new fully data-backed `/admin/command` route alongside the existing admin UI. It must use FastAPI/PostgreSQL as the source of truth and preserve existing lead, booking, funnel, content, and analytics records instead of duplicating them. The approved design is at `docs/superpowers/specs/2026-08-12-command-workspace-design.md`. It includes CRM Contacts, Tasks, Smart Plans, Opportunities, Marketing, internal Agreement/Templates/files, Reports, Listings/Search/Map, Websites, and server-side compliant/auditable AI actions. The interaction model uses the archived Command UI layouts, but branding must remain Sold With Sweeney & Co. and must not copy Keller Williams branding. Internal agreement lifecycle tracking is approved; real DocuSign API and legally binding in-house signatures are not in first release.
- 2026-04-21: Reviewed the live spec + implementation to produce a current feature inventory for the app, including chatbot, booking, funnels, analytics, admin tools, scroll/video experiences, and the Zapier-based KW Command CRM handoff.
- 2026-04-21: CRM integration should be described as a Zapier webhook flow rather than a direct Keller Williams API integration.
- 2026-04-21: The booking system description should explicitly mention Google Maps travel-time checks for in-person meetings so only feasible time slots are shown based on Brandon's surrounding calendar commitments and the requested location.
- 2026-04-21: The homepage hero eyebrow badge now reads `Northeast Association of REALTORS® Realtor Of The Year 2025` per the latest copy request.
- 2026-04-21: Hardcoded Sold With Sweeney email references now use `info@soldwithsweeney.com`, including public contact copy, internal notification recipient defaults, and the seeded default admin email.
- 2026-04-20: The chatbot booking chooser label was changed from `Google Meet` back to `Video Call`, and the guided booking prompt copy was updated to match.
- 2026-04-20: `Book Brandon` now opens the chatbot in guided booking mode again, with `Phone Call`, `Video Call`, and `In Person` visible before slot selection.
- 2026-04-20: Same-day booking availability now filters out any slot whose start time is already past in Eastern time, and booking validation rejects already-started times server-side too.
- 2026-04-18: Book Brandon CTAs now open the persistent chatbot directly into a `next_available` calendar picker mode; a browser smoke test confirmed the homepage CTA opened chat and displayed live next available slot buttons from `/api/v1/booking/available-slots`.
- 2026-04-18: Fixed the Vercel frontend typecheck blocker by replacing JSX-derived React keys in the About page with stable string ids and typing buyer journey steps as `ReactNode`; `npm run typecheck` and `npm run build` both pass locally.
- 2026-04-18: Updated the chatbot booking calendar card so if Brandon has no availability on the selected date, it scans the next 10 business days and displays up to 6 next available time options.
- 2026-04-13: Updated the investor calculator offer cap from 70% to 80%, renamed the card to `80% Rule Offer Cap`, and removed prefilled deal values so the instant snapshot and full-report gate only appear after users enter a complete deal.
- 2026-04-11: Fixed the investor calculator short-term loan bug. Terms of 1-2 years now model interest-only bridge / fix-and-flip debt, while 3+ year terms stay amortized; regression coverage lives in `backend/tests/test_investor_metrics.py`.
- 2026-04-10: Wrote the durable notification queue design at `docs/superpowers/specs/2026-04-10-brandon-notification-queue-design.md`.
- 2026-04-10: Wrote the implementation plan at `docs/superpowers/plans/2026-04-10-brandon-notification-queue.md`.
- 2026-04-10: Implemented the notification queue itself, including route integrations for leads, chat leads, funnels, booking attempts, booking confirmations, seller evaluator usage, seller ratings, investor report requests, and investor calculator engagement.
- 2026-04-10: Hardened the notification queue so routes commit user actions and pending notification jobs before immediate delivery is attempted, which prevents Brandon from getting false-positive emails for rolled-back work.
- 2026-03-23: Fixed Railway Railpack/Caddy detection bug by adding root `railway.json` and modifying `backend/Dockerfile` for root context. Fixed missing phase images by force-adding to git. Verified backend health success.
- 2026-03-27: Analyzed chatbot flow, then implemented structured assistant actions/buttons in the chatbot with browser-verified action rendering and booking-widget launch.
- 2026-04-02: Applied About/Buy/Sell/home-page polish pass and removed the homepage hero headshot poster to fix the initial video flash.
- 2026-04-02: Added the provided Brandon and Paige gala image into `frontend/public/headshots/` and wired it into the About page.
- 2026-04-09: Home `TrustSection` now uses a two-card left review rail and includes a new Madison Levanti Google testimonial sourced from `reviews.html`.
- 2026-04-09: `frontend/src/components/buyer/BuyerMistakes.tsx` is now a rotating deck with auto-advance, hover-to-pause, and click-to-flip `Buyer Mistake` / `The Fix` states; browser automation verified both reorder and flip behavior.
- 2026-04-10: Added admin Google Calendar OAuth bootstrap endpoints and settings-page connect UI. Booking now truthfully blocks with a one-time authorization message until Brandon connects Calendar.
- 2026-04-10: Recalibrated seller valuation baselines so the Lowell smoke-test sample now returns roughly `$512k-$602k` instead of overshooting the local market band.
- 2026-04-10: Investor calculator now exposes its instant metrics up front and only gates the full AI report behind contact capture.
- 2026-04-10: Seller calculator now stores every calculation plus the follow-up expectation rating in the database through analytics events.
- 2026-04-10: Browser verification of the investor calculator was run against localhost with screenshots saved as `investor-instant-results-check.png` and `investor-full-report-check.png`.
- 2026-04-09: Revised `BuyerMistakes.tsx` again so the right side is now a single rotating hero card instead of four stacked cards, and removed the extra `Rotating Buyer Playbook` explainer panel.
- Next: If requested, extend the action system with lead-capture prompts, richer analytics events, or additional widget types beyond booking, or swap in an awards-collage image once that asset is added.

## 2026-03-31 - Home Videos
- Updated Invest and Sell pages with custom watermark-free hero videos saved in `frontend/public/videos`.

## 2026-03-31 - Home Video Cards
- Replaced static fallback images with video loops in the AudienceCards section (Buy, Sell, Invest). Used the corresponding videos from each respective page's hero section.

## 2026-03-31 - Marketing Dyson Sphere
- Built a 3D WebGL dyson sphere using Three.js inside `frontend/src/components/sell/MarketingSphere.tsx`.
- Maps custom PNG image logos onto 2D canvas sprites inside the 3D scene.
- Load `mini_house.gltf` into the center.
- Wrapped in Next.js `useRef` and `useEffect` with strict `cancelAnimationFrame` and unmounting logic for safely avoiding WebGL context leaks during client router navigation.
Added interactive videos to Seller Staging Checklist
- 2026-04-09: Fixed BuyerMistakes animation to slide purely upwards (y axis) instead of using rotateX.
- 2026-04-09: Refactored BuyerMistakes to move pagination dots to vertical alignment on the right edge of the card, moving from a top-horizontal layout.
- 2026-04-10: Mapped 'Style beds' staging checklist item to its respective interactive media.
- 2026-04-11: Updated the footer logo to use the new SWS Primary Logo White and Gold TRANSPARENT.
- 2026-04-11: Introduced interactive bio-panel hover images on the About page 'Built on Trust' section.
- 2026-04-11: Updated text split layout for 'REALTOR Of The Year' statistic on the About page.
- 2026-04-11: Aligned visibility styling for designation logos in the home page TrustSection with the About page.
- 2026-04-11: Updated the About page team image to an animated carousel featuring 3 team shots.
- 2026-04-11: Implemented SVGLoader and ExtrudeGeometry for all nodes on the MarketingSphere on the Sell page, making them true 3D.
- 2026-04-11: Fixed SVG colors in MarketingSphere and correctly aligned node text sprites to the global Y axis.
- 2026-04-28: Added Cookie Consent component to the main layout which persists consent state in localStorage.
- 2026-04-28: Added a silent client-side IP-based location check to the homepage Hero component (via geojs.io) that switches the background video to the high-res Dracut Drone video if the user's city is strictly 'Dracut', and 'Andover_drone.mp4' if the city is 'Andover'.
- 2026-05-10: Added 4-strategy investor toggle (Buy & Hold / STR / Flip / BRRRR) at top of /invest with URL sync via ?strategy=. Each strategy has its own input field set, default values, and result panel layout.
- 2026-05-10: Added RentalAnalyzerModal that estimates monthly rent (LTR) or nightly rate (STR) from condition + upgrade heuristics layered on top of RentCast's AVM. New backend route POST /api/v1/investor/estimate-rent.
- 2026-05-10: Refactored frontend/src/lib/investor-calc.ts into a discriminated union with 4 calc functions (calculateBuyHoldMetrics / calculateStrMetrics / calculateFlipMetrics / calculateBrrrrMetrics). All exhaustively type-checked.
- 2026-05-10: Added Vitest to frontend (no test framework existed before). 31 unit tests covering all 4 strategies + parseInputs gate.
- 2026-05-10: Calibrated STR market multipliers against AirDNA/AirROI/Rabbu — Tourist 2.0× (was 3.0×), Urban 1.3× (was 2.4×), Suburban 1.2× (was 1.8×). Initial multipliers were peak-season optimistic; new values match annualized data. Calibration appendix documented in docs/superpowers/specs/2026-05-10-investor-strategy-toggle-and-rental-analyzer-design.md.
- 2026-05-11: Rental analyzer v2 — property-type aware. RentCast comps are re-weighted by property-type similarity before computing the baseline, plus 5 new heuristic adjusters (property type, amenities, bath count, year built, sqft) layer on top. Modal grows two new chip groups (radiogroup-accessible). property_type is required. Smoke test: duplex vs apartment building at same address delta = +15.4%.
- 2026-06-01: Hermes/Atlas agent-control foundation is live on Railway. Railway operations may use `scripts/railway-sweeney` only after its currently loaded account/workspace token passes live access to `enchanting-perception`; the separately supplied 2026-08-26 rollout credential was Member-verified but was not saved into that file. A direct CLI session has the same project-listing requirement. The backend bearer token file is `.env.agent-control.local`, is gitignored by `.env.*.local`, and must not be printed.
- 2026-06-01: Agent bridge routes live under `/api/v1/agent-control/*` and are gated by `AGENT_CONTROL_ENABLED=true` plus bearer `AGENT_CONTROL_TOKEN`. Current production capabilities are `status.read`, `leads.recent.read`, and `bookings.recent.read`; responses mask emails/phones and audit allowed calls to `agent_action_audits`.
- 2026-06-01: Railway env changes made with `--skip-deploys` require `railway redeploy` for the running backend to pick up the new env snapshot. A plain `restart` left `/agent-control/status` returning 503 `Agent control is not configured`; redeployment `73deb46c-433f-4824-b87d-4f7fed63a154` fixed it.
- 2026-06-01: Production verification for agent control: authorized `/status` returns `status=ok` and the expected capabilities; unauthenticated `/status` returns 401; `/actions`, `/leads/recent?limit=1`, and `/bookings/recent?limit=1` all return successfully. Keep future checks from printing lead/booking details; summarize counts only.
- 2026-06-01: Known unrelated production issue remains in blog auto-generation: Railway logs show `gemini-3-pro-preview` is no longer available. Treat as separate Gemini model update work, not part of the Hermes bridge.
- 2026-06-01: `atlas-agent` is live in Railway project `enchanting-perception` as service ID `6dc65984-89c1-400c-9d17-d5412befd031` at `https://atlas-agent-production-99dc.up.railway.app`. Health returns `status=ok`, `gateway=stopped`; stopped is expected until provider and Telegram setup.
- 2026-06-01: Hermes deploy source was the external template repo `praveen-ks-2001/hermes-agent-template` at commit `7224d7c1a4dcffe9304f49bc843f55716f5561b4`, uploaded with `railway up --path-as-root` because GitHub-backed service creation was unauthorized with the then-current account/workspace token.
- 2026-06-01: Hermes volume is `atlas-agent-volume` (`594c4970-ba61-4888-8372-57d0c235db65`) mounted at `/data`. Admin creds are in ignored `.env.hermes-admin.local` and Railway variables only. `BRANDON_BACKEND_URL` plus masked `BRANDON_AGENT_CONTROL_TOKEN` are set on `atlas-agent` for future backend-control skills.
- 2026-06-02: Hermes is now configured with `LLM_MODEL=gemini-3.5-flash` and the existing Gemini API key via the Hermes admin API. Official Gemini docs identify `gemini-3.5-flash` as the stable model string. Live Gemini API test returned HTTP 200, Hermes status reports Gemini configured, and `/health` reports gateway running.
- 2026-06-02: Hermes still has no messaging platforms configured. Logs warn that no messaging platforms are enabled and unauthorized users are denied. Need Telegram bot token + Brandon Telegram user ID/approval before Brandon can chat with Hermes.
- 2026-06-02: CRM assumption changed: Brandon does not use KW CRM for the future and is moving to eXp. Treat KW/Zapier as legacy one-way context only; do not build new KW CRM automation. Need eXp CRM/API/Zapier details before CRM work.
- 2026-06-02: Google Workspace / Gmail / Drive / Docs / Sheets setup should be authorized as `brandon@soldwithsweeney.com`. Do not assume `soldwithsweeneyfordeployment@gmail.com` or `rishabnandibusiness@gmail.com` is the Workspace identity for Brandon-side email and document automation.
- 2026-06-02: Adding `rishabnandibusiness@gmail.com` to Brandon's Google Cloud project IAM was blocked by Domain Restricted Sharing (`constraints/iam.allowedPolicyMemberDomains`). Next access path should use an allowed internal Workspace principal such as `brandon@soldwithsweeney.com` or a temporary org-policy exception created by a Workspace/Cloud organization admin.
- 2026-06-02: `gcloud` is authenticated as `brandon@soldwithsweeney.com` and configured to project `sold-with-sweeney-website-v1` (`721277606944`). Enabled Workspace APIs: Calendar, Gmail, Drive, Docs, Sheets, People, Pub/Sub, Workspace Events, Drive Activity, Tasks, Admin SDK, Meet, Chat, Slides, Forms. Enabled Maps/site APIs: Maps JavaScript, Places legacy/new, Geocoding, Directions, Distance Matrix, Routes, Static Maps, Street View Image, Time Zone, Address Validation.
- 2026-06-02: Brandon approved broad Workspace access for Hermes/Atlas. Implemented and deployed a full-access Workspace OAuth connector with routes under `/api/v1/workspace/*`, durable token key `google_workspace_refresh_token`, and Settings UI connect/reconnect controls. The connector requests Gmail full mail access, Calendar, Drive, Docs, Sheets, Slides, Forms, Contacts/Directory, Tasks, Chat, Meet, and Admin SDK scopes. Latest live backend deployment `fc779bef-f978-4cee-92b2-4056765e4d8b` succeeded; `/workspace/status` is configured but not connected until Brandon consents. This grants capability only after Brandon consents; autonomous send/delete/edit tools still need explicit backend agent-tool implementation and confirmation policy.
- 2026-06-02: Live Workspace OAuth is connected as `brandon@soldwithsweeney.com`; `/api/v1/workspace/status` returns `configured=True`, `connected=True`, `can_connect=True`. Next blocker for Brandon using Hermes is Telegram channel setup: need BotFather token and Brandon's numeric Telegram user ID, then wire Hermes channel allowlist and backend Workspace action tools.
- 2026-06-02: Current verified Hermes/Atlas state is not PRD-complete yet, but the first Workspace action-tool slice is live. Backend deployment `9e351dd9-fa15-45da-b7e9-8f4947a8f948` exposes `workspace.status.read`, `workspace.drive.search`, `workspace.gmail.draft.create`, `workspace.docs.create`, `workspace.sheets.append`, and `workspace.gmail.send` under `/api/v1/agent-control/workspace/*`; direct Gmail send requires explicit Brandon confirmation. Workspace OAuth remains connected as `brandon@soldwithsweeney.com`. Remaining work beyond Telegram token/user ID: configure Hermes channel/allowlist and expand action tools for Calendar/Contacts/deeper Gmail/Drive reads before claiming full executive-assistant automation.
- 2026-06-02: Atlas/Hermes backend deeper Workspace action-tool slice is live on Railway deployment `047f12b5-2a3c-4488-90af-f8dde7d8f080`: `workspace.gmail.search`, `workspace.gmail.thread.read`, `workspace.drive.file.read`, `workspace.calendar.events.read`, `workspace.calendar.event.create`, and `workspace.contacts.search`. Live authenticated status/actions smoke returned 15 capabilities/actions with no missing new IDs, Workspace remained connected, and unconfirmed calendar event creation returned 422. Calendar event creation requires `confirmed_by_brandon=true`; audit metadata excludes Gmail bodies, Drive file contents, contact addresses/phones, calendar descriptions, and attendee addresses. Still need Telegram channel/allowlist and Hermes-side tool invocation wiring.
- 2026-06-02: Hermes-side Atlas MCP bridge is now in repo at `hermes/atlas_backend_mcp.py` and deployed in the custom `atlas-agent` image as `/app/atlas_backend_mcp.py` on Railway deployment `8dcd567b-0c27-4eda-a7ef-46104aff91fe`. The patched Hermes template writes `mcp_servers.atlas_backend` with 16 allowlisted backend tools when `BRANDON_BACKEND_URL` and `BRANDON_AGENT_CONTROL_TOKEN` are present; Railway variable-name check confirmed both exist. Local stdio smoke against production returned Workspace `configured=True` and `connected=True`; Hermes health is running. Railway SSH remains blocked by project role (`MEMBER` required), and Telegram still needs BotFather token plus Brandon numeric Telegram user ID before chat-level MCP tool use can be tested.
- 2026-06-05: Telegram channel is configured for Hermes `atlas-agent` using bot username `soldwithsweeney_bot` and Brandon's allowlisted Telegram user ID `8647590834`. Railway deployment `0636f515-da58-4b94-a6ca-bb27ef2ed8f5` reached `SUCCESS`; Hermes `/health` reports gateway running; native dashboard status reports `gateway_platforms.telegram.state=connected` with no error; setup status reports Telegram configured after mirroring vars into Hermes persistent config. BotFather token was pasted in chat, so rotate it after final live Telegram chat testing and update Railway/Hermes with the replacement token.
- 2026-06-05: Hermes/Atlas assistant is now branded as Sydney. Telegram Bot API reports visible display/first name `Sydney` while username remains `soldwithsweeney_bot`; Hermes default profile `SOUL.md` was updated through the protected dashboard API to identify the assistant as Sydney, and the gateway restarted cleanly. `/start` is ignored as a platform ping once a session exists; use `/reset` or a normal message like `who are you?` to verify the new persona in Telegram.
- 2026-06-05: If Sydney still answers "Hermes Agent", the cause is Hermes session-level system prompt persistence, not Telegram bot metadata. The live fix was to set `agent.system_prompt` to a Sydney identity overlay in Hermes raw config, delete Brandon's stale Telegram session `20260605_135856_d86a6a6f`, and restart the gateway. After the restart, `/health` was running, native Telegram state was connected, and there were zero remaining Brandon Telegram sessions.
- 2026-08-01: A live Keller Williams audit found the old brokerage is still systemic despite the `/links` bio already saying eXp: global navigation/footer graphics and copy, Home/About/Buy/Sell/Invest/Blog copy, all 13 published blog articles, all 9 published funnels, the investor-report broker disclaimer, the `/links` background and KW valuation URL, and AI/seed prompts that can regenerate it. No production content was changed. Current videos, animation frames, blog hero images, and published funnel hero images were visually/OCR-checked and did not contain Keller Williams branding. A failed read-only `psql` command echoed the production database connection URL in tool output, so rotate the database password; no credential or database mutation was performed.
- 2026-08-01: The eXp Realty brokerage swap is implemented on `codex/exp-brokerage-swap` without changing the Sold With Sweeney black-and-gold design or introducing unrelated company branding. Active frontend copy/assets, footer legal destinations, backend prompts/defaults, link-pack seed, and legacy CRM wording now use eXp/provider-neutral language; four KW-branded logo composites were removed and the official white eXp logo was added with provenance. A guarded dry-run-first migration covers current persisted content and predicts 33 updates (13 blogs, 16 funnels, 1 content block, 1 link pack, 2 link items) with zero remnants. Release backend first, apply and verify the migration second, then verify the frontend so the permanent legacy-blog redirect cannot land before its renamed destination. Frontend tests/typecheck/build and 39 branding-focused backend tests pass; an isolated `origin/main` comparison confirms the release has the same eight non-release full-suite failures: seven notification timing assertions and one local-PostgreSQL dependency.
- 2026-08-01: Local review used the frontend at `http://localhost:3000` with a preview-only API at `127.0.0.1:8000`. Both the shared header and `/links` background are intentionally Sold With Sweeney-only; eXp identification remains in the appropriate profile copy and footer disclosures, with its logo used only in the footer. The preview read the actual published link-pack snapshot and assets once through an explicit read-only transaction, closed it without a write, and applied the eXp text/destination substitutions in memory while preserving all six top-level items, groups, profile/photo, socials, thumbnails, destinations, and active states. Navbar and `/links` regression tests plus the full 33-test frontend suite, TypeScript, targeted ESLint, structure comparison, HTTP checks, and browser/console review pass; both temporary local processes were stopped before the production build.
- 2026-08-01: Production rollout of the eXp swap was explicitly authorized. GitHub `main` is connected to both Railway project `enchanting-perception` production and Vercel Production. Fresh preflight passed, and the final read-only migration preview rolled back cleanly with 33 planned updates and zero remnants. Keep the unrelated `docs/deployment/hermes-railway.md` edit out of the release; exact live deployment/migration results belong in the deployment handoff.
- 2026-08-01: The eXp brokerage swap is live on production from GitHub `main` commit `b364124`. Railway deployment `0a580dbf-b570-4f2c-9500-c416531c3e53` and Vercel Production deployment `5710309885` succeeded. The guarded migration applied 33 updates and saved a private backup at `/Users/rishabnandi/.sws-backups/exp-branding/exp-branding-backup-20260802T024423Z.json`; the final production dry-run reports 0 planned updates and 0 old-brand remnants. A TDD follow-up fixed a false positive caused by PostgreSQL `BYTEA` values arriving as `memoryview(format='c')` while packaged assets are `bytes`. Live backend health is OK; Home/About/Buy/Sell/Invest/Blog/Links return 200 with no Keller Williams text; the header and `/links` are SWS-only, the footer retains eXp identification/legal links, and the real six-item published links page is intact.
- 2026-08-02: Fixed the production SWS header logo regression from commit `3358610`. The white/gold PNG contains large transparent margins, so the former `76x40` contract produced only about `52x21px` of visible artwork. The navbar now crops those margins inside a `112x64px` mobile / `124x64px` desktop frame while rendering the source at `72px` / `80px` tall, producing an approximately `117x46px` visible desktop mark without changing the 64px header or adding eXp branding. A red-green regression test, all 34 frontend tests, TypeScript, targeted lint, production build, local browser measurement, and live custom-domain HTML verification passed. Vercel Production deployment `5715079788` and Railway deployment `3a9fee4a-a042-40c0-8e92-bf7a60196b83` succeeded; the unrelated Hermes document edit stayed excluded.
# 2026-08-12: Internal Command workspace continuation
- Bulk permitted-data import is now available at `POST /api/v1/command/contacts/import` (maximum 1,000 rows per request). It deduplicates by email and records per-contact import activity.
- Contact workspaces now expose actual linked opportunity records rather than a blank opportunities array. This is internal CRM data only; no external brokerage or e-signature archive is present in this repository or database.
- Opportunity details support real contact, vendor, and offer relationship writes through the internal API; they are not mock UI state.
- Smart Plan workspaces now create steps and enroll contacts through the same internal database APIs.
- Agreement lifecycle is now audit-backed: creation/status changes/recipient additions create agreement events, detail workspaces show recipients/events/files, and a migration adds an optional agreement link to private file assets.
- Marketing, website, and report Command views now query live internal content, funnels, and aggregate analytics event-type records instead of rendering only counters.
- Listing map placement is now coordinate-based. The backend geocodes only when the configured Google Maps key is present and returns explicit errors otherwise.
- 2026-08-12: Command migration `fb74d2c0a611` is applied in the configured internal database; verified `crm_file_assets.agreement_id`. Authenticated runtime reads across overview/reports/marketing/websites/listings/agreements returned HTTP 200. Frontend full suite has 40 passing tests and production build completes; local static generation still logs an unrelated unavailable content-block API fetch.
- 2026-08-12: Recovered local permitted account archive is located at `Documents/Codex/2026-07-27/realtime-voice-chat/outputs/authorized-account-archive`. Migration `fe2a1c4b8d75` created an immutable `crm_archive_artifacts` catalog; all 12,580 source files are checksum-indexed in the configured internal PostgreSQL database and visible through the protected `/admin/command/archive` browser. This is evidence preservation, not proof that every rendered source count became a distinct normalized CRM row.
- 2026-08-12: Recovered DocuSign list pages normalized to 117 unique internal agreement records, two template records, and 151 private archive-file metadata rows. The source has 155 rendered agreement rows across four captured pages; duplicate agreement titles were intentionally collapsed while source artifact evidence remains immutable in the catalog.
- 2026-08-12: `ff7d8e1a9234` adds private `BYTEA` storage for each recovered archive artifact. `backend/scripts/import_recovered_archive_bytes.py` is resumable and checksum-verifies every local source before writing it; the protected Command archive page downloads bytes only via an authenticated API endpoint. Do not call the archive migration complete until the database has non-null bytes for all 12,580 catalog rows.
- 2026-08-12: Archive-byte migration completed in the configured internal PostgreSQL database: all 12,580 catalog rows have non-null source bytes (745,060,261 total). Spread checksum/length probes passed and an authenticated Command endpoint download of a 956,385-byte DocuSign artifact returned 200 and the exact expected length. The old local archive remains an independent recovery copy; database storage is now the application source of truth.
- 2026-08-12: The full Command workspace/archive implementation was merged into local `main` at `dc9f258`. Merged-checkout validation passed: 25 focused backend tests, 35 frontend Command API tests, and the production build. The feature worktree/branch was removed after integration; remote publishing was not requested in the selected local-merge workflow.
- 2026-08-12: Migration merge revision `f0c8a6d9e431` resolves the Command archive's two Alembic heads. It is applied to the configured database, which now reports that sole head. Post-merge authenticated archive retrieval returned HTTP 200 and the exact expected byte count; all 12,580 artifacts have internal bytes.
- 2026-08-12: Production release completed via merged GitHub PR #1 (`2fc88cf`). Vercel serves `/admin/command/archive` with HTTP 200 and Railway exposes the protected archive list/content endpoints. An authenticated production list reported 169 DocuSign artifacts; a production download matched its cataloged 956,385-byte size exactly.
- Contacts UI now fetches the entire paginated CRM dataset in 100-row batches rather than only the first 50 results.
- Smart Plan enrollment lifecycle now supports active/paused/completed status changes with a server-backed pause/resume UI.
- Opportunity workspace now persists pipeline stage changes and refreshes from the internal API after movement.
- Contact workspace now updates lead/client stages through the internal API and appends a stage-change timeline activity.
- Smart Plan steps can now be edited through a persisted API update from the plan workspace.
- Command shell now has a responsive mobile navigation drawer on nested module routes.
- Added and applied `fc0e8a4b9422` for generic task-to-internal-record links. API path: POST `/api/v1/command/tasks/{task_id}/links`.
- The Tasks workspace now exposes the persisted task-link workflow for contacts, opportunities, agreements, and listings.
- Latest Command branch verification: 46 frontend tests, frontend typecheck/build, and 8 focused Command backend tests pass. Build retains the unrelated non-fatal local content-block fetch warning.
- Agreement detail UI now uploads private files directly to the selected agreement through the existing protected storage endpoint.
- Contact workspace now supports direct idempotent tag creation and assignment.
- Agreement state transitions are now server-validated and forward-only, with terminal completed/voided/expired states protected.
- Agreement templates now expose a persisted body update API for template-editor UI work.
- Agreement template editor UI now creates and edits internal template bodies through the authenticated Command API.
- Task links now have a retrieval API and are visible from the task workspace.
- Tasks now support server-side status and due-date filtering with matching workspace controls.
- Listings now support persisted active/pending/sold/withdrawn lifecycle status changes.
- Contact profile fields now support persisted edits with timeline activity auditing.
- Sweeney AI briefing UI now explicitly renders provenance, review-required status, loading, and backend errors.
- Tasks UI now captures persisted priority and due datetime at creation and displays stored due dates.
- Referrals are now a persisted internal Command module (`crm_referrals`) with source, optional contact linkage, and a validated lifecycle. Migration `fd1c8e9a4703` is applied to the configured database; authenticated reads of referrals and core Command summaries return 200.
- 2026-08-12 verification: focused Command backend tests (11), Command frontend API tests (15), TypeScript, and the optimized frontend build pass. The DB has 51 contacts and 52 activities projected from existing internal leads; the newly provisioned CRM tables remain empty. Do not describe KW Command or DocuSign data as imported: no external archive is present in this repository or database.
- 2026-08-12: Command Home supports a current-month private birthday/anniversary queue backed by persisted optional contact dates. Migration `a2d7e4b9c118` is applied; runtime celebrations read returned 200. Verification after this slice: 12 focused Command backend tests, 16 frontend Command API tests, frontend TypeScript, and production build pass (same unrelated local content-block fetch warning during static generation).
- Contact profile editing exposes the optional persisted birthday and anniversary fields, and Saved Searches display their stored criteria in the contact workspace. Frontend API tests now have 17 passing cases and TypeScript passes.
- Contacts API and workspace now support server-side name/email/phone search plus lifecycle-stage filtering with bounded pagination; runtime authenticated reads for query/stage filters returned 200. Focused tests: 12 backend, 18 frontend API, TypeScript passing.
- Listings & Map now supports persisted server-side address search and status filtering, with the same filter state applied to listing cards and computed map pins. Authenticated listing filter reads returned 200; focused verification is 12 backend tests, 19 frontend API tests, and TypeScript.
- Tasks now have validated lifecycle/priority mutations and a persisted complete editor for title, details, priority, and due date. Task status accepts only open/in_progress/completed/cancelled; invalid authenticated update returned 422. Focused verification: 13 backend tests, 20 frontend API tests, TypeScript passing.
- Task updates now append immutable contact-timeline `task_updated` activity with a concise changed-field summary. The task audit helper is covered in the focused Command suite (14 passing backend tests).
- Referrals workspace now supports linking a referral to any internal contact through a complete paginated contact chooser and visibly resolves the linked contact on the referral card. Backend referral contact-existence validation is retained; current focused checks are 14 backend, 20 frontend API, and TypeScript passing.
- Contact workspace now exposes read-only internal booking history via lead-ID first and case-insensitive email fallback, preserving bookings as the authoritative source. The typed client is used for contact workspace reads. Runtime verified contact workspace includes a bookings array; focused verification is 14 backend tests, 21 frontend API tests, and TypeScript passing. A local regenerated `.next` cache was deleted only to reclaim temp-disk space.
- Reports now supports bounded, read-only individual-card detail lists for contacts, leads, open tasks, opportunities, agreements, and analytics events; summary also moved behind the typed Command API client. Runtime contacts report-detail returned a matching metric and no more than 25 rows. Focused checks: 14 backend tests, 22 frontend API tests, TypeScript passing.
- Command Home has a persisted Goals workflow (target, current value, validated cadence) with POST/GET/PATCH APIs and real progress rendering. DB migration `b7e1f2d4a906` is applied; GET /goals runtime returned 200. Focused verification: 15 backend tests, 23 frontend API tests, TypeScript passing.
- Goal management UI is complete on Command Home: create name/target/cadence and update progress directly from each card through persisted authenticated API writes. Frontend API tests (23), TypeScript, and diff check pass.
- Smart Plans now have a distinct validated plan lifecycle (active/paused/archived), editable in the plan workspace without changing steps/enrollments. Invalid lifecycle mutation returned 422. Current focused checks: 15 backend tests, 24 frontend API tests, TypeScript passing.
- Opportunities now validate the complete internal pipeline vocabulary and audit genuine stage changes with immutable `opportunity_stage_changed` activity. Invalid stage mutation returned 422; 16 focused Command backend tests pass.
- Sweeney AI Briefing UI now uses the shared typed Command API client for deterministic preview and explicit generation. Backend retains aggregate-only/review-required/non-autonomous constraints and audit logging on generation. Verification: 16 backend tests, 25 frontend API tests, TypeScript passing.
- Contact action bar now uses the typed Command client for notes, saved searches, tags, and assignments. Saved searches now persist self-describing contact workspace criteria instead of `{}`. Verification: 16 backend tests, 26 frontend API tests, TypeScript passing.
- Broad current-branch Command regression: 60 frontend tests, 16 Command backend tests, migration head b7e1f2d4a906, and optimized frontend build all pass. Build includes all Command routes; it retains the known non-fatal local content-block fetch refusal when no public API is running.
- Agreements can now be created from a selected persisted internal template (`template_id`), with server-side contact/template existence validation. This remains internal tracking/review only. Current verification: 16 backend tests, 27 frontend API tests, TypeScript and diff check pass.
- Opportunity contact relationship editor now loads every paginated internal contact and resolves relationship records to names, avoiding raw-ID-only CRM UI. Existing relationship API remains authoritative. Frontend API tests (27), TypeScript, and diff check pass.
- Opportunity contact relationship writes are now idempotent per contact+role; duplicates return the existing association and different roles remain distinct. Covered by 17 focused Command backend tests and diff check.
- PostgreSQL now enforces unique opportunity/contact/role rows via migration c3a8b5e1d204, protecting concurrent writes beyond API idempotency. Migration applied; focused Command backend suite has 18 passing tests.
- Task links now validate allowed internal entity types and target-record existence before persistence, preventing arbitrary/dangling links. Focused Command backend suite has 19 passing tests.
- Internal contact imports now accept optional birthday and anniversary dates and retain them on the canonical CRM contact record, feeding the existing celebrations workflow. Focused Command backend suite has 20 passing tests.
- Smart Plan enrollment no longer depends on raw contact IDs in the workspace. The API returns display names, writes are idempotent, and PostgreSQL enforces one enrollment per plan/contact through migration e8f2c4a6b901. Verification: 21 Command backend tests, 28 API tests, and TypeScript pass.
- Task record links no longer require raw internal IDs. Users select real contacts, opportunities, agreements, or listings; APIs return display names and both API logic and migration ab4d9e2c7108 prevent duplicates. Verification: 22 backend tests, 62 frontend tests, and TypeScript pass.
- Agreement creation now uses a paginated internal contact selector alongside the persisted template selector, so contact relationships are created from canonical CRM records. Verification: 63 frontend tests and TypeScript pass.
- Tasks now expose their canonical contact relationship at creation and after creation, with contact selectors instead of IDs. Backend rejects unknown contacts and supports deliberate unassignment. Verification: 22 backend tests, 64 frontend tests, and TypeScript pass.
- A protected Command contact-import page now supports permitted CSV/JSON archives, client-side schema preview, 1,000-row bounded batches, and created/duplicate reconciliation. Server-side email dedupe is case-insensitive. Verification: 22 backend tests, 65 frontend tests, and TypeScript pass.
- Contact profile writes now preserve explicit nulls for optional email, phone, birthday, and anniversary fields, so profile clears really remove stale CRM data. Verification: 23 backend tests, 65 frontend tests, and TypeScript pass.
- Referral creation and actual status changes now generate immutable timeline activity for the linked CRM contact, making referral history visible from contact workspaces. Verification: 24 focused backend tests pass.
- The public homepage now skips an unconfigured localhost content fetch during production static generation and uses its authored fallback. Verification: 65 frontend tests, TypeScript, and clean 33-route build pass; only a stale disposable 1.6 GB Next cache was removed to free build space.
- Saved Searches is now a complete Command workspace: it lists all saved contexts with linked contacts and stored criteria, and lets admins explicitly remove entries. Verification: 25 backend tests, 66 frontend tests, and TypeScript pass.
- Contact notes, tags, and saved searches now have native inline workspace controls with local loading/error state and refresh the contact data in place instead of using browser prompts or full-page reload. Verification: 66 frontend tests, TypeScript, and clean 34-route build pass.
- Contact profile editing now uses a native inline validated form with date inputs and explicit optional-field clearing, eliminating the final prompt-driven profile workflow. Verification: 66 frontend tests, TypeScript, and clean 34-route build pass.
- Opportunity stage changes and contact links now generate contact-scoped immutable CRM activity, so a linked contact sees pipeline history in its timeline instead of only a detached global event. Verification: 25 focused backend tests pass.
- Agreement creation/status moves and Smart Plan enrollment/status moves now add contact-scoped immutable CRM activity, completing cross-workspace timeline visibility for those relationship events. Verification: 25 backend tests, 66 frontend tests, and TypeScript pass.
- Contact notes and tags now support scoped removal with contact ownership checks, in-place refresh, and immutable removal activity on the contact timeline. Verification: 26 backend tests, 67 frontend tests, TypeScript, and clean 34-route build pass.
- Archive intake now accepts a structured permitted JSON bundle spanning contacts, tasks, notes, opportunities, referrals, listings, templates, and agreements. It resolves email-linked contact relations internally, reports unresolved references, and deduplicates canonical records. Verification: 27 backend tests, 68 frontend tests, TypeScript, and clean 34-route build pass.
- Task editing now uses a native validated modal with date controls, saving and error feedback rather than browser prompts. The Link Pack layout no longer downloads five Next Google fonts during production builds, and the 34-route build is clean. Verification: 68 frontend tests and TypeScript pass.
- Archive intake now includes a downloadable JSON template covering every supported CRM collection and email relationship key, so permitted source archives can be prepared for direct import. Verification: 68 frontend tests, TypeScript, and clean 34-route build pass.
- 2026-08-13: Command Contacts Task 10 is frozen in `.worktrees/command-full-parity` with a synthetic 366-row, stateful, auth-bound, fail-closed Playwright fixture; no live backend/database/private archive was used. Exact browser coverage now includes directory/detail states, 8+3 reciprocal tab/panel relationships, stateful mutations and uncertain-write reconciliation, mobile containment/44px targets, four axe states, privacy-safe manifest validation, and strict zero-pixel Contacts snapshots on Darwin and Playwright 1.62.1 Linux. Fresh evidence: independent isolated Playwright 70/70; scoped desktop 30/30, mobile 10/10, accessibility 16/16; final scoped Vitest 321/321; typecheck, scoped lint, build, diff-check, Darwin visual 14/14, and Linux visual 14/14. Both independent reviewers approved with no Critical/Important findings. The access-controlled private manifest comparison remains explicitly NOT RUN because `COMMAND_VISUAL_SOURCE_MANIFEST` is unset; never commit its path or pixels. The user explicitly waived that comparison for this release and authorized the verified branch for PR, merge, and production deployment.
- 2026-08-13: Command Contacts shipped through PR #3 at merge commit `eca0b02a8b61dee464efcf22771b0b527196a0cd`; the matching Vercel production build and Railway deployment `4cb8083f-8d86-4da8-b504-f0fc25d62a4f` succeeded. A protected PostgreSQL 17 logical backup was validated outside the repo, then production migrated sequentially from `f0c8a6d9e431` to sole head `7d1f3a5b6c8e`. Authenticated production API and browser smokes passed for directory/detail/summary/timeline/evidence/neighbors, including 401/403 boundaries and zero browser/HTTP 5xx errors. The directory currently exposes 362 SWS contacts. The private archive Contacts reconciliation/import and private source screenshot comparison were not run; capture-position/section counts remain zero and must not be described as private-data parity.
