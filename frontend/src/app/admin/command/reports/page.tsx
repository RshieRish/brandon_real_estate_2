'use client';
import {useEffect,useState} from 'react'; import {ChartBar} from '@phosphor-icons/react';
export default function Page(){const [data,setData]=useState<Record<string,number>|null>(null);useEffect(()=>{const t=localStorage.getItem('admin_token');fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:8000'}/api/v1/command/reports/summary`,{headers:{Authorization:`Bearer ${t}`}}).then(r=>r.json()).then(setData)},[]);return <div className="min-h-[100dvh] bg-[#080807] p-6 text-white"><main className="mx-auto max-w-6xl"><p className="text-xs uppercase tracking-[.25em] text-[#eac469]">Internal reporting</p><h1 className="mt-1 text-3xl font-black">Reports</h1><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{data?Object.entries(data).map(([label,value])=><section key={label} className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><ChartBar className="text-[#eac469]" size={21}/><p className="mt-7 text-3xl font-black">{value.toLocaleString()}</p><p className="mt-1 text-xs uppercase tracking-widest text-white/45">{label.replace('_',' ')}</p></section>):Array.from({length:6}).map((_,i)=><div key={i} className="h-36 animate-pulse rounded-2xl bg-white/5"/>)}</div></main></div>}
