'use client';
import {useEffect,useState} from 'react'; import {useParams} from 'next/navigation';
import { ContactActions } from '@/components/command/ContactActions';
const tabs=['Timeline','Opportunities','Smart Plans','Tasks','Notes','Saved Searches'] as const;
export default function Page(){const {contactId}=useParams<{contactId:string}>();const [data,setData]=useState<any>(null),[tab,setTab]=useState<typeof tabs[number]>('Timeline');useEffect(()=>{const token=localStorage.getItem('admin_token');fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:8000'}/api/v1/command/contacts/${contactId}/workspace`,{headers:{Authorization:`Bearer ${token}`}}).then(r=>r.json()).then(setData)},[contactId]);if(!data)return <div className="min-h-[100dvh] bg-[#080807] p-8 text-white">Loading contact workspace…</div>;const map:any={Timeline:data.timeline,Opportunities:data.opportunities,'Smart Plans':data.smart_plans,Tasks:data.tasks,Notes:data.notes,'Saved Searches':data.saved_searches};return <div className="min-h-[100dvh] bg-[#080807] p-6 text-white"><main className="mx-auto max-w-5xl"><p className="text-xs uppercase tracking-[.25em] text-[#eac469]">Contact workspace</p><h1 className="mt-1 text-3xl font-black">{data.contact.first_name} {data.contact.last_name}</h1><div className="mt-3 flex flex-wrap gap-2">{data.tags?.map((tag:any)=><span key={tag.id} className="rounded-full bg-[#eac469]/15 px-2 py-1 text-xs text-[#eac469]">{tag.name}</span>)}</div><ContactActions contactId={contactId}/><div className="mt-7 flex gap-2 overflow-auto">{tabs.map(t=><button onClick={()=>setTab(t)} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm ${tab===t?'bg-[#eac469] text-black':'bg-white/5 text-white/60'}`}>{t}</button>)}</div><section className="mt-5 rounded-2xl border border-white/10 bg-white/[.035] p-5">{map[tab].length?map[tab].map((x:any)=><article key={x.id} className="border-b border-white/10 py-4 last:border-0"><b>{x.summary??x.title??`Plan #${x.plan_id}`}</b><p className="mt-1 text-sm text-white/45">{x.body??x.kind??x.status??''}</p></article>):<p className="py-10 text-center text-white/40">No {tab.toLowerCase()} for this contact.</p>}</section></main></div>}
