'use client';
import {useEffect,useState} from 'react'; import {MapPin,Plus} from '@phosphor-icons/react'; import {commandApi,type Listing} from '@/lib/command/api';
export default function Page(){const [items,setItems]=useState<Listing[]>([]),[address,setAddress]=useState('');useEffect(()=>{void commandApi.listings().then(setItems)},[]);async function add(){if(!address)return;const item=await commandApi.createListing({address,latitude:null,longitude:null});setItems(x=>[item,...x]);setAddress('')}return <div className="min-h-[100dvh] bg-[#080807] p-6 text-white"><main className="mx-auto max-w-5xl"><p className="text-xs uppercase tracking-[.25em] text-[#eac469]">Internal inventory</p><h1 className="mt-1 text-3xl font-black">Listings & Map</h1><div className="mt-6 flex gap-3"><input value={address} onChange={e=>setAddress(e.target.value)} placeholder="Add listing address" className="flex-1 rounded-xl border border-white/10 bg-white/5 p-3"/><button onClick={add} className="rounded-xl bg-[#eac469] px-4 text-black"><Plus/></button></div><div className="mt-6 grid gap-4 lg:grid-cols-[1fr_.8fr]"><div className="space-y-2">{items.map(i=><div key={i.id} className="rounded-xl border border-white/10 bg-white/[.035] p-4"><b>{i.address}</b><p className="mt-1 text-xs text-white/40">{i.status}</p></div>)}</div><div className="grid min-h-64 place-items-center rounded-2xl border border-dashed border-[#eac469]/30 bg-[#eac469]/5 text-center text-white/45"><div><MapPin className="mx-auto text-[#eac469]" size={32}/><p className="mt-3">Map pins appear when internal listings have coordinates.</p></div></div></div></main></div>}
