'use client'; import {useEffect,useState} from 'react'; import {FileText,Plus} from '@phosphor-icons/react'; import {commandApi,type Agreement} from '@/lib/command/api';
export function AgreementsWorkspace(){const [items,setItems]=useState<Agreement[]>([]),[title,setTitle]=useState('');useEffect(()=>{void commandApi.agreements().then(setItems)},[]);async function add(){if(!title)return;const created=await commandApi.createAgreement({title,contact_id:null});setItems(x=>[created,...x]);setTitle('')}return <div className="min-h-[100dvh] bg-[#080807] p-6 text-white"><main className="mx-auto max-w-5xl"><p className="text-xs uppercase tracking-[.25em] text-[#eac469]">Internal agreement tracking</p><h1 className="mt-1 text-3xl font-black">Agreements</h1><p className="mt-2 text-sm text-white/50">Lifecycle and files are internal records; this is not legal e-signature execution.</p><div className="mt-6 flex gap-3"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Agreement title" className="flex-1 rounded-xl border border-white/10 bg-white/5 p-3"/><button onClick={add} className="rounded-xl bg-[#eac469] px-4 text-black"><Plus/></button></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{items.map(i=><article key={i.id} className="rounded-2xl border border-white/10 bg-white/[.035] p-5"><FileText className="text-[#eac469]" size={22}/><h2 className="mt-4 font-bold">{i.title}</h2><span className="mt-3 inline-block rounded-full bg-[#eac469]/15 px-2 py-1 text-xs text-[#eac469]">{i.status}</span></article>)}</div></main></div>}
