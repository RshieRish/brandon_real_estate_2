'use client';
import { useEffect, useState } from 'react';
import { Check, Plus, WarningCircle } from '@phosphor-icons/react';
import { commandApi, type Task } from '@/lib/command/api';

export function TasksWorkspace() {
  const [tasks, setTasks] = useState<Task[]>([]); const [title, setTitle] = useState(''); const [status, setStatus] = useState('all'); const [dueBefore, setDueBefore] = useState(''); const [error, setError] = useState<string | null>(null); const [selected, setSelected] = useState<number | null>(null); const [links, setLinks] = useState<Record<number, { id:number; entity_type:string; entity_id:number }[]>>({}); const [entityType, setEntityType] = useState('opportunity'); const [entityId, setEntityId] = useState('');
  const load = () => commandApi.tasks({ status: status === 'all' ? undefined : status, due_before: dueBefore ? `${dueBefore}T23:59:59Z` : undefined }).then(setTasks).catch((err) => setError(err.message)); useEffect(() => { void load(); }, [status, dueBefore]);
  async function add() { if (!title.trim()) return; try { const task = await commandApi.createTask({ title: title.trim(), description: '', priority: 'normal', contact_id: null, due_at: null }); setTasks((all) => [...all, task]); setTitle(''); } catch (err) { setError(err instanceof Error ? err.message : 'Unable to create task'); } }
  async function complete(task: Task) { try { const updated = await commandApi.updateTask(task.id, { status: task.status === 'completed' ? 'open' : 'completed' }); setTasks((all) => all.map((item) => item.id === task.id ? updated : item)); } catch (err) { setError(err instanceof Error ? err.message : 'Unable to update task'); } }
  async function showLinks(taskId: number) { try { const rows = await commandApi.taskLinks(taskId); setLinks((all) => ({ ...all, [taskId]: rows })); } catch (err) { setError(err instanceof Error ? err.message : 'Unable to load task links'); } }
  async function linkTask() { if (!selected || !Number.isInteger(Number(entityId)) || Number(entityId) < 1) return; try { await commandApi.addTaskLink(selected, entityType, Number(entityId)); await showLinks(selected); setEntityId(''); setSelected(null); } catch (err) { setError(err instanceof Error ? err.message : 'Unable to link task'); } }
  return <div className="min-h-[100dvh] bg-[#080807] p-6 text-white"><main className="mx-auto max-w-5xl"><p className="text-xs uppercase tracking-[.25em] text-[#eac469]">Internal CRM</p><h1 className="mt-1 text-3xl font-black">Tasks</h1><div className="mt-6 flex gap-3"><input value={title} onChange={(event) => setTitle(event.target.value)} onKeyDown={(event) => event.key === 'Enter' && add()} placeholder="Add a task" className="flex-1 rounded-xl border border-white/10 bg-white/5 px-4 py-3" /><button onClick={add} className="rounded-xl bg-[#eac469] px-4 text-black"><Plus size={19} /></button></div><div className="mt-4 flex flex-wrap gap-3"><select value={status} onChange={(event) => setStatus(event.target.value)} className="rounded-lg border border-white/10 bg-black/40 p-2 text-sm"><option value="all">All statuses</option><option value="open">Open</option><option value="completed">Completed</option></select><input value={dueBefore} onChange={(event) => setDueBefore(event.target.value)} type="date" className="rounded-lg border border-white/10 bg-black/40 p-2 text-sm" aria-label="Due by" /></div>{error && <p className="mt-4 flex gap-2 text-red-300"><WarningCircle size={18} />{error}</p>}<div className="mt-6 space-y-2">{tasks.map((task) => <div key={task.id} className="rounded-xl border border-white/10 bg-white/[.035] p-4"><div className="flex items-center gap-4"><button onClick={() => complete(task)} aria-label={`Toggle ${task.title}`} className={`grid h-6 w-6 place-items-center rounded-full border ${task.status === 'completed' ? 'border-[#eac469] bg-[#eac469] text-black' : 'border-white/30'}`}>{task.status === 'completed' && <Check size={15} />}</button><span className={task.status === 'completed' ? 'text-white/35 line-through' : 'font-medium'}>{task.title}</span><button onClick={() => setSelected(task.id)} className="ml-auto text-xs font-bold text-[#eac469]">Link record</button><button onClick={() => showLinks(task.id)} className="text-xs text-white/55">Show links</button><span className="text-xs uppercase text-white/45">{task.priority}</span></div>{links[task.id]?.length ? <p className="mt-3 text-xs text-white/45">{links[task.id].map((link) => `${link.entity_type} #${link.entity_id}`).join(' · ')}</p> : null}</div>)}{!tasks.length && <p className="rounded-xl border border-dashed border-white/15 p-10 text-center text-white/40">No matching tasks.</p>}</div>{selected && <div className="fixed inset-0 z-40 grid place-items-center bg-black/70 p-4"><section className="w-full max-w-sm rounded-2xl border border-white/10 bg-[#12110f] p-6"><h2 className="text-lg font-bold">Link internal record</h2><div className="mt-4 grid gap-3"><select value={entityType} onChange={(event) => setEntityType(event.target.value)} className="rounded-lg bg-black/40 p-3 text-sm"><option value="opportunity">Opportunity</option><option value="agreement">Agreement</option><option value="listing">Listing</option><option value="contact">Contact</option></select><input value={entityId} onChange={(event) => setEntityId(event.target.value)} inputMode="numeric" placeholder="Internal record ID" className="rounded-lg bg-black/40 p-3 text-sm" /></div><div className="mt-5 flex justify-end gap-3"><button onClick={() => setSelected(null)} className="text-sm text-white/60">Cancel</button><button onClick={linkTask} className="rounded-lg bg-[#eac469] px-4 py-2 text-sm font-bold text-black">Link</button></div></section></div>}</main></div>;
}
