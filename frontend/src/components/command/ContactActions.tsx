'use client';
import { commandApi } from '@/lib/command/api';
export function ContactActions({contactId}:{contactId:string}){const id=Number(contactId);async function createNote(){const body=window.prompt('Note text');if(!body)return;await commandApi.createContactNote(id,body);window.location.reload()}async function saveSearch(){const name=window.prompt('Saved search name');if(!name)return;await commandApi.createContactSavedSearch(id,name,{contact_id:id,scope:'contact_workspace',saved_from:'command'});window.location.reload()}async function addTag(){const name=window.prompt('Tag name')?.trim();if(!name)return;const tag=await commandApi.createTag(name);await commandApi.assignContactTag(id,tag.id);window.location.reload()}return <div className="mt-4 flex flex-wrap gap-2"><button onClick={createNote} className="rounded-lg border border-white/15 px-3 py-2 text-xs text-white/70 hover:border-[#eac469]">Add note</button><button onClick={saveSearch} className="rounded-lg border border-white/15 px-3 py-2 text-xs text-white/70 hover:border-[#eac469]">Save search</button><button onClick={addTag} className="rounded-lg border border-white/15 px-3 py-2 text-xs text-white/70 hover:border-[#eac469]">Add tag</button></div>}
