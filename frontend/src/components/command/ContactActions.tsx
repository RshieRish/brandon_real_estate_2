'use client';
export function ContactActions({contactId}:{contactId:string}){async function create(path:string,label:string){const value=window.prompt(label);if(!value)return;const token=localStorage.getItem('admin_token');const body=path==='notes'?{body:value}:{name:value,criteria:{}};await fetch(`${process.env.NEXT_PUBLIC_API_URL??'http://localhost:8000'}/api/v1/command/contacts/${contactId}/${path}`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify(body)});window.location.reload()}return <div className="mt-4 flex gap-2"><button onClick={()=>create('notes','Note text')} className="rounded-lg border border-white/15 px-3 py-2 text-xs text-white/70 hover:border-[#eac469]">Add note</button><button onClick={()=>create('saved-searches','Saved search name')} className="rounded-lg border border-white/15 px-3 py-2 text-xs text-white/70 hover:border-[#eac469]">Save search</button></div>}
