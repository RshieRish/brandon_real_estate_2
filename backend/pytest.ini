[pytest]
markers =
    live_llm: integration tests that hit real Gemini API (require GEMINI_API_KEY)
asyncio_mode = auto
# live_llm tests cost real API calls; opt in with `-m live_llm` to run them.
addopts = -m "not live_llm"
